# ThreadSnap 领域语境

ThreadSnap 领域描述从圈子版块自动发现或从已知帖子 URL 清单导入链接，并将每次执行结果保存为独立快照的业务概念。

## Language

**采集配置体系**：
由每周计划节点、循环计划节点、自动提取规则、平台采集配置、来源分组和平台圈子来源共同组成的分层配置；两类计划节点只决定何时使用哪条规则，规则只保存定时提取业务参数，平台运行参数和采集来源各自只有一个归属。
_Avoid_: 单圈全量配置、监控配置

**循环计划节点**：
在所选星期的同一自然日开始时刻到结束时刻之间，按正整数分钟间隔展开实际触发点并引用一条或多条自动提取规则的计划节点；开始时刻始终触发，结束时刻只有被间隔序列恰好命中时才触发。
_Avoid_: 持续循环、跨午夜窗口、停机补跑、把间隔写入自动提取规则

**圈子发现提取**：
按提取配置访问指定圈子和版块，翻页发现帖子 URL，再交给统一详情采集流程处理。
_Avoid_: URL 清单导入、持续监控

**跨论坛聚合帖**：
平台明确返回在当前圈子来源有序列表中、但详情稳定原始论坛身份属于另一论坛的帖子；发现来源与原始归属分别保存，只要列表、URL、帖子ID及列表声明的原始归属与详情一致，它就是当前来源的有效快照结果。
_Avoid_: 错帖、跨平台帖子、按详情原始论坛改写发现来源

**后续平台接入验收样本**：
汽车之家或易车新增适配器验收前，由项目从已确认的社区页面出发，取得最新回复和最新发布实际入口，发现、规范化、去重并在运行前冻结的500个当前有效且互不重复的帖子详情URL；它按平台分别形成验收分母，用于验证新增适配器，不替代懂车帝既有2000条技术选型与目标Linux部署门禁。
_Avoid_: 要求甲方另交2000条清单、两个平台合计500条、重复URL补量、运行后删除失败样本、把500条结果外推为2000条吞吐证明

**后续平台功能样本集**：
项目从对应平台冻结的500条验收样本和社区发现过程自行分层形成的功能验证子集；按实际可观察情况覆盖普通正文、图片、视频、评论数量档位、状态、认证和平台控制，并同时记录未观察到的场景，不以甲方另填功能样本表作为接入前置。
_Avoid_: 把推测状态写成期望真值、为凑场景伪造样本、用少量功能样本替代500条整体验收

**URL 清单提取**：
导入一组已知帖子 URL 直接创建手动提取批次，不经过圈子列表发现，但与圈子发现提取复用同一平台详情采集流程。
_Avoid_: 圈子 URL、性能测试结果文件

**内部验证版**：
仅在受控内部环境中使用，用于验证懂车帝平台采集与基础提取闭环的第一阶段交付。
_Avoid_: 公网版、正式版

**正式版**：
内部验证版通过后的可选系统集成阶段；只有明确选择公网部署时，才必须包含身份认证、角色权限和外围访问保护。
_Avoid_: 测试版、内部验证版

**可选公网部署**：
第一版完成后，只有客户明确需要公网访问时才启动的部署增强；复用客户现有身份与权限系统，并增加公网网关和外围保护，不属于第一版默认范围或验收条件。
_Avoid_: 第一版必做、公网直出、直接开放 VNC/CDP

**提取批次**：
一次手动、每周定时或循环计划提取执行及其结果集合；循环计划批次使用独立触发类型，并只进入独立侧边栏入口下的“循环计划列表”，手动与每周定时批次进入“提取列表”。两类列表和详情复用同一组件与处理能力，不同批次彼此独立，不覆盖或合并。
_Avoid_: 监控记录、同步记录、口碑巡检批次

**垂媒口碑巡检**：
按固定车型范围从一个或多个垂直媒体平台取得口碑分、排名、口碑量、口碑评价篇数、差评率及同期页面证据，并形成可与既有有效快照比较的周期性业务快照；它与论坛帖子链接提取是两个独立业务模块。
_Avoid_: 帖子提取、持续跟踪帖子、把指标结果作为帖子批次的附属视图

**口碑巡检批次**：
一次正式日程触发或失败项补跑形成的垂媒口碑巡检执行及其不可变结果集合；它拥有独立的批次身份和详情视图，不属于提取批次。测试环境的合成验收运行使用隔离身份和数据源，不属于正式巡检批次。
_Avoid_: 提取批次、截图标签、覆盖上一期指标

**月末巡检批次**：
每个自然月最后一天代替当日日常巡检而创建的唯一口碑巡检批次；它只采集一次全量车型与平台数据，同时承担前一日比较、全量页面证据和当日汇报的数据来源。
_Avoid_: 与日常巡检重复执行、只做月度汇总、不参与前一日比较

**每日正式巡检批次**：
系统按日程为一个自然日创建的唯一完整口碑巡检批次；普通日期创建日常巡检，月末最后一天由月末巡检批次替代，当前版本不通过通用“立即巡检”创建第二个同日完整批次。
_Avoid_: 人工完整巡检、同日多个正式批次、映射验证、失败项补跑

**口碑合成验收运行**：
仅在显式测试运行模式和隔离测试数据库中，由“手动运行测试”按钮选择确定性场景后创建的口碑处理链测试对象；它使用虚拟指标和测试证据验证比较、网页颜色、XLSX颜色、证据与汇报，不访问平台、不进入正式日期幂等键、不成为前日基线，也不在生产环境暴露。
_Avoid_: 生产立即巡检、写入业务数据库、用合成结果替代真实首跑、TXT颜色、调用真实平台适配器

**口碑巡检批次根列表项**：
巡检批次列表中代表一个每日正式巡检批次及其全部关联补跑的顶层行；它按计划巡检日期定位当天正式结果，同时分别展示原批次真实状态与关联结果完整度，补跑只在行内展开或详情查看。
_Avoid_: 补跑与正式批次同级铺开、按实际创建时间错排延迟批次、用关联完整度覆盖原批次状态

**口碑巡检批次类型徽标**：
批次列表与详情标题中区分“日常巡检”和“月末巡检”的中性类型标识；基线初始化和同日补触发作为附加徽标，成功、部分成功、失败继续由独立状态语义表达。类型徽标不拆分列表、不创建导航，也不通过整行底色表达。
_Avoid_: 月末独立列表、月末二级导航、用类型颜色冒充状态颜色、整行染色干扰指标涨跌

**口碑巡检批次详情视图**：
口碑巡检批次内以URL状态切换的“排名数据”“页面证据”“汇报结果”三个同级视图；行主体默认进入排名数据，证据或汇报摘要可直接深链到对应视图，刷新和返回保持当前选择。
_Avoid_: 无URL状态标签、所有入口强制同一页、把指标异常只藏在证据或汇报中

**口碑排名数据平台列组**：
排名数据表按批次实际平台动态生成的固定六列组合：口碑分、排名、口碑量、口碑评价篇数、差评率、状态或证据；角色、车系、车型作为左侧吸附身份列，前日值、差值和比较状态收敛在指标单元格内，不拆成额外列。
_Avoid_: 预留未接入平台空列、每个指标拆出多列、允许拖散平台列组、把网页第四列写入XLSX指标合同

**基线初始化批次**：
系统没有可用前日比较基线时形成的首份真实巡检批次；它可以是首次每日正式巡检，也可以是从完整真实验证结果冻结的真实验收初始化批次。它不计算变化或着色，对批次冻结的全部车型平台组合全量留证，并成为严格下一自然日巡检的前日基线。
_Avoid_: 普通日检变化截图、伪造零变化、额外初始化任务、与月末批次重复

**新映射基线**：
一个车型首次进入某平台，或其平台稳定车型身份相对前日发生变化时，在普通批次内为该车型平台项建立的新指标序列起点；该项首日不计算差值或着色并保存指标区域证据，其他身份连续的执行项仍正常比较。
_Avoid_: 整批重新初始化、跨平台车型身份强行续比、只有重点车型留证、把URL改名等同稳定身份变化

**口碑巡检固定日程**：
当前版本按 `Asia/Shanghai` 每日10:00创建每日正式巡检批次；批次到达可靠终态后立即生成对应汇报，不另设汇报时点。月末最后一天同一10:00触发月末巡检批次，巡检时间只读且不属于帖子提取每周计划。
_Avoid_: 用户可编辑时间、复用帖子每周计划节点、按服务器本地时区解释

**同日延迟巡检批次**：
服务不可用导致10:00口碑巡检未触发时，系统在同一 `Asia/Shanghai` 自然日恢复后、确认当日尚无每日正式巡检批次的前提下立即补建的唯一正式批次；它沿用当日普通或月末类型，并以巡检日期和计划批次类型幂等去重，跨过自然日后只记录漏触发而不补采历史页面。
_Avoid_: 跨日补拍、同日重复正式批次、把配置错误当停机补触发、用恢复时间伪装计划时间

**计划时点口碑巡检范围**：
每日正式巡检按计划时刻10:00查询发布历史后冻结的已发布口碑巡检范围版本；同日延迟补触发仍使用10:00当时生效的版本，10:00后发布的版本只影响下一次正式巡检，计划时点没有已发布版本时记录配置错误且不创建批次。
_Avoid_: 延迟批次读取恢复时当前版本、事后发布范围回填当天批次、没有范围时创建空批次

**前日比较基线**：
当前口碑巡检日期的前一个自然日中，对应车型、平台和指标的最终有效巡检结果；前一自然日缺少有效结果时基线为空，不跨日改用更早结果。
_Avoid_: 最近一次成功值、跨日回退基线、把多日变化标为较昨日变化

**冻结前日基线**：
口碑巡检批次创建时保存的前一自然日关联结果版本及其指标快照；创建前完成的补跑可以进入该基线，创建后完成的晚到补跑不追溯改变本批次的比较、证据触发或汇报输入。
_Avoid_: 查询时动态基线、晚到补跑自动重算历史、无人操作时改变已发汇报

**口碑指标变化**：
一个车型在一个平台上的单项口碑分、排名、口碑量、口碑评价篇数或差评率相对前日比较基线的变化方向；各指标独立成立，方向冲突时表达为表现分化，不合成为整车总体提升或下降。
_Avoid_: 整行统一着色、加权总评、用单个指标代表整车趋势

**排名范围身份**：
平台口碑排名所属榜单或分类的稳定身份，由平台显示名称、分类或稳定榜单ID/URL证明；排名数值只有在前后两日范围身份相同时才可比较，范围变化记为 `not_comparable`。
_Avoid_: 只保存名次数值、跨榜单比较、把换榜单判为排名升降

**口碑指标比较状态**：
一个当前指标与冻结前日基线之间是否可以比较的状态；无前日有效值为无基线，排名范围变化为 `not_comparable`，两者都不产生升降颜色，后者作为排名口径变化单独汇报和留证。
_Avoid_: 无基线当作不变、跨范围排名升降、用颜色掩盖口径变化

**巡检全量页面证据**：
每个新建口碑巡检批次都以冻结范围内全部启用的“车型 × 平台”执行项为证据分母，不再按重点或竞品角色、指标变化、普通日检或月末类型筛选截图对象；每项在同一浏览器页面上下文取得指标和唯一指标区域PNG。
_Avoid_: 按变化缩小证据分母、先HTTP后为同一项重复打开页面、历史批次事后补拍、把目标81写成当前单平台分母

**指标区域证据**：
一个需要证据的口碑巡检执行项在取得指标的同一页面上下文中，按连续三次稳定的DOM边界直接截取的唯一不可变PNG；它保留车型身份、口碑分、排名、口碑量及必要上下文，并记录来源URL、冻结映射、采集时间、页面尺寸、截图矩形、截图尺寸和SHA-256，供前端、XLSX和证据包共同复用。
_Avoid_: 完整长截图、独立重访平台、OCR拼图、同一执行项重复业务截图

**口碑页面证据包**：
一个口碑巡检批次按冻结的全量车型平台证据范围在首次下载时生成并缓存的独立ZIP；批次终态先冻结证据清单和输入哈希，ZIP原样包含已持久化的指标区域PNG、`manifest.json` 与 `SHA256SUMS`，缺失证据只记录原因，不生成占位图，也不与TXT或XLSX混装。
_Avoid_: 批次终态自动压缩全部ZIP、打包阻塞终态汇报、重新访问或重绘、重压缩原始PNG、缺失项假图

**口碑逻辑证据进度**：
以一个“车型 × 平台”执行项为一份证据的完成统计；只有唯一指标区域PNG写入并通过SHA-256校验时该逻辑项才完整，列表和详情显示相同逻辑分母。
_Avoid_: 当前27项显示为54份、把指标成功等同证据完整、用静态81替代批次冻结分母、隐藏失败平台分布

**口碑巡检证据状态**：
指标区域证据的处理状态与指标可用状态分别表达；必需证据失败不抹掉可靠指标，但使口碑巡检批次总体为部分成功，只能通过新关联补跑形成新时点证据。
_Avoid_: 证据失败等同指标失败、事后补拍原批次、用占位假图补成完整

**口碑巡检范围版本**：
一组可用于未来口碑巡检的车型、重点或竞品角色及各平台稳定页面映射快照；范围变化产生新版本，口碑巡检批次冻结创建时版本，后续配置变化不改写历史批次及其证据分母。
_Avoid_: 前端硬编码车型、覆盖历史范围、删除配置时删除历史批次

**口碑巡检范围配置**：
口碑巡检模块内部唯一编辑车型身份、重点或竞品角色、平台车型映射及其版本的配置区域；它只读引用共享平台连接状态，不重复编辑平台 Session、帖子提取规则或固定巡检日程。
_Avoid_: 帖子提取配置标签、重复平台凭证、可编辑10:00巡检时间、独立汇报时点

**口碑范围初始化清单**：
首次为全新业务数据库建立27款车型草稿的人工复核UTF-8 CSV输入，固定包含Schema版本、稳定初始化键、车系、车型展示名、重点或竞品角色、组内顺序，以及当前平台代码、车型ID、页面URL和平台展示名。清单只由一次性初始化命令导入，数据库保存源文件校验值而不保存CSV副本；附件截图只作人工核对参考，不能通过OCR直接建立长期车型身份。
_Avoid_: 截图OCR直接建库、前端或采集器硬编码27款、导入即视为映射验证通过、为一次初始化建设长期上传页面

**口碑范围展示顺序**：
已发布范围版本保存的完整车型显示顺序；重点或竞品是每款车型的角色属性，不再用于详情页把车型重新分组。草稿中的车型与映射顺序发布后由未来批次冻结，并原样决定排名数据和页面证据的行序。
_Avoid_: 临时前端排序冒充配置、填写排序数字、筛选状态拖拽局部结果、历史批次跟随新顺序

**已发布口碑巡检范围**：
用户在全部当前接入平台映射通过门禁后显式发布、可被未来每日正式巡检冻结的唯一范围版本；验证通过的草稿本身不生效，发布新版本只替代未来批次使用的当前指针。
_Avoid_: 验证后自动生效、多个当前版本、未发布草稿进入10:00批次

**口碑范围发布影响摘要**：
用户发布范围版本前后可见的变更与生效说明，包含新增、停用、角色及映射变化、下一正式巡检生效时间、预计车型平台分母和新映射基线数量；它只描述未来批次，不把版本应用到已创建批次或重算当天结果。
_Avoid_: 只显示“保存成功”、隐藏分母变化、立即应用到运行中批次、延迟补触发读取事后版本

**口碑首发全量复核**：
发布第一个口碑巡检范围版本时，在既有发布确认框中对27款车型身份、重点或竞品角色、组内顺序和当前平台映射进行的增强人工核对；每行显示当前验证状态、时间和证据入口，全部27项绑定当前映射验证通过且用户明确确认后才允许首发。
_Avoid_: 只核对汇总数字、验证后修改仍可发布、另建审批系统、双人签名、默认勾选确认

**口碑范围本地脏状态**：
“车型与映射”页面中尚未成功提交到服务端草稿的浏览器内编辑状态；站内离开时必须允许保存草稿、放弃本地改动或继续编辑，刷新或关闭时使用浏览器未保存提示。已经成功保存但尚未发布的服务端草稿不属于本地脏状态，可以离开并在返回后继续。
_Avoid_: 离开即自动发布、已保存草稿也反复拦截、保存或验证进行中静默丢弃、发布时遗漏本地改动

**口碑巡检车型身份**：
一个业务统计车型不可变的内部身份，承载车系、展示名称、项目组归属和重点或竞品角色；同名但属于不同代际或不同业务统计对象的车型使用不同身份，前日比较只在同一车型身份内进行。内部ID由服务端生成或由初始化清单提供平台中立的稳定键，不编码任一平台代码或平台车型ID；新增时必须手动填写1至80个字符的项目组归属且表单和接口均不提供默认值。车型行的修改入口复用同一对话框，可修改车系、车型、项目组、角色和当前平台映射，但不可修改内部车型ID。保存时去除首尾空白并随范围版本冻结，不通过名称推断或自动合并；列表单元格保持只读显示，当前既有车型统一归属“奇瑞项目组”。
_Avoid_: 车型名称主键、平台车型ID充当内部身份、按字符串自动合并、同名换代车型复用身份

**平台车型映射**：
一个口碑巡检车型身份在某个平台上的稳定车型 ID、页面 URL 和平台展示名称关联；名称变化只产生新映射版本，候选匹配必须经用户明确确认，不自动决定车型身份。
_Avoid_: 名称即映射、自动跨平台合并、平台改名时改写历史

**平台映射批量粘贴**：
“车型与映射”页面中一次只为一个平台粘贴一组“内部车型ID、平台车型ID、页面URL、平台展示名”的草稿编辑方式；保存前预览新增、修改和错误，提交范围全有或全无，未包含车型保持不变。保存只形成未验证映射，真实页面验证逐项产生成功或失败结果。
_Avoid_: 按名称自动匹配、多平台混合粘贴、部分保存错误批次、粘贴即发布或即视为验证通过

**已验证平台车型映射**：
在同一次受控页面访问中同时证明车型身份一致、三个指标可解析或明确暂无，并稳定生成唯一指标区域证据的平台车型映射；验证记录绑定适配器版本、时间、原始文字、矩形和文件校验值。
_Avoid_: 仅HTTP成功、只核对车型名称、选择器缺失当作暂无、重复三次平台访问

**平台映射验证有效性**：
验证记录对内部车型绑定、平台代码、平台车型ID、页面URL、平台展示名和映射验证合同版本的有效绑定；任一绑定内容或合同版本变化、或真实页面稳定车型身份不一致时失效。验证不按经过天数自动过期，角色、顺序、Session变化及单次网络或认证失败不撤销仍绑定当前内容的既有验证。
_Avoid_: 固定7天或30天过期、瞬时访问失败抹掉既有证据、角色排序触发重验、内容变化沿用旧验证

**映射验证失败项重试**：
一次批量映射验证结束后只对终态失败项创建的新验证尝试；暂时网络或页面超时在每次操作内最多自动重试一次，认证失败等待Session更新，确定性车型身份错误必须先修改映射。每次尝试保留内容哈希、时间、阶段结果和证据，不覆盖历史，也不构成口碑巡检批次。
_Avoid_: 默认重跑成功项、身份错误原URL循环请求、跨尝试拼接门禁结果、验证重试触发汇报

**映射待修复**：
平台车型页面不存在、跳转到其他车型或稳定身份校验不一致后形成的平台映射状态；系统停止继续访问已确认失效的旧 URL，可以提供候选，但只有用户确认并验证后才能生成新范围版本。
_Avoid_: 自动改绑候选、持续请求失效URL、用同名页面替换

**口碑巡检平台范围**：
一个口碑巡检批次创建时实际已经接入并纳入本功能的平台集合；当前阶段只包含已接入的一个平台，未来平台接入不会扩张历史批次，三个目标平台全部接入后才启用全平台映射完整门禁。
_Avoid_: 把目标平台当作当前已接入平台、新平台自动改写历史批次、当前阶段固定三平台分母

**口碑巡检执行项**：
一个口碑巡检批次中针对单个“车型 × 平台”组合的执行单元；单项在有界自动重试后可以独立成功或失败，不因自身失败终止其他执行项。
_Avoid_: 整批唯一任务、失败后复用旧值、一个车型失败即终止批次

**口碑巡检实时进度**：
运行中批次已经完成终态判定并提交数据库的车型平台执行项数量；每项成功、部分成功或无需再重试的失败都立即增加进度，暂时错误在自动重试结束前不计入。排名数据和页面证据随单项事务同步可见，批次状态保持运行中，汇报只在全部执行项终态后生成。
_Avoid_: 浏览器任务启动数、整批结束后从零跳满、首轮暂时错误提前计失败、用运行中结果生成汇报

**口碑巡检运行预算**：
当前单平台阶段由后端固定执行的有界时间合同：单次车型页面访问绝对上限90秒，暂时性错误最多自动重试1次，口碑巡检批次从实际开始执行起最多运行45分钟；批次预算耗尽时未完成项记为 `timeout` 并立即汇总终态。
_Avoid_: 前端可编辑超时、无限重试、从计划10:00倒扣延迟批次预算、等待所有页面永久挂起

**口碑巡检平台并发快照**：
口碑巡检批次创建时冻结的共享平台内部并发值；它决定该平台同时处理的车型平台执行项数量，指标请求、页面打开、截图和即时重试共用一个执行项槽位，并与帖子提取合计服从同一平台总容量。
_Avoid_: 单独口碑并发配置、运行中修改并发、请求和截图重复占槽、绕过平台1至8安全范围

**正式口碑巡检非抢占优先级**：
每日正式口碑巡检进入平台队列后优先领取随后释放的容量槽位，但不取消或中断已经开始的帖子请求；口碑批次终态后，暂缓领取的帖子任务按原FIFO顺序继续，失败项补跑和映射验证不享有该优先级。
_Avoid_: 抢占运行中请求、补跑插队、改变帖子原有相对顺序、跨平台全局阻塞

**口碑巡检认证失败项**：
执行时发现共享平台Session失效后快速结束并标记为 `auth_required` 的车型平台项；口碑巡检批次不进入长期认证等待，用户在共享平台配置完成认证后通过“重新巡检失败项”形成关联补跑。
_Avoid_: 暂停45分钟等待人工、认证后覆盖原批次、为口碑巡检复制Session编辑入口、把认证失败记为普通超时

**口碑巡检批次状态**：
按批次全部执行项的真实终态汇总：全部执行项成功为成功，存在部分成功或失败项且仍有可靠结果为部分成功，没有任何执行项取得可靠结果为失败；成功结果在其他执行项异常时仍保持有效且不可变。
_Avoid_: 单项失败等同整批失败、丢弃部分成功结果、用旧值补成成功

**口碑巡检补跑批次**：
终态口碑巡检批次由用户发起的关联后续巡检，只包含原关联链中仍未成功的车型平台执行项；它拥有独立批次身份，不覆盖原批次，补跑成功项不再进入下一次补跑。
_Avoid_: 自动重试、覆盖原巡检批次、重复补跑已成功项

**关联口碑巡检结果**：
一个原口碑巡检批次及其全部补跑批次按车型平台执行项汇总形成的当前结果视图；每个车型平台只选择一个完整执行项版本，完整成功优先于部分成功、部分成功优先于失败，同等级取最新一次，不跨尝试拼接指标或证据，也不物理合并或改写贡献批次。
_Avoid_: 跨时点拼接三个指标、数据库合并批次、把原批次状态改成成功、删除失败历史

**口碑巡检删除墓碑**：
每日正式巡检批次及其关联补跑被人工整体删除后保留的最小来源记录；它保存原批次编号、巡检日期、计划批次类型、删除时间、原结果哈希和日期幂等身份，不保留已删除指标、证据或文件，并继续占用该日期正式批次位置，阻止删除后重建或同日补触发。
_Avoid_: 恢复已删除数据、删除后重算后续比较、释放日期幂等键、保留完整隐藏副本

**口碑巡检删除作业**：
按不可变删除清单把一个每日正式巡检关联链的文件先移动到同文件系统隔离区，再以数据库事务删除领域数据并写入墓碑的可恢复两阶段操作；数据库提交前失败必须恢复文件和原数据，提交后隔离区清理失败只进入可重试的“存储清理待重试”，不撤销逻辑删除。
_Avoid_: 数据库与文件交错直接删除、提交后恢复业务数据、部分产物永久删除后仍显示正常、重复删除创建第二个作业

**口碑指标可用状态**：
口碑分、排名、口碑量、口碑评价篇数或差评率的可用状态：来源明确证明平台暂无时为 `not_available`，采集或解析未取得可靠结论时为 `unknown`；两者都没有可比较数值，但前者是已确认事实，后者是待处理异常。未开售或零评价车型只有在页面身份正确、全部口碑指标为空、页面服务端状态同时明确总评分和评价人数为零，且差评率接口再次确认评价总数为零时，才属于可成功提交的 `not_available`。
_Avoid_: 统一显示斜杠、把解析失败记为平台暂无、用旧值填充

**平台展示指标值**：
平台页面当日向用户可见的口碑分、排名或口碑量原始文字及其十进制规范值；变化只按该可见精度比较，格式或单位等价但规范值相同时不算变化，不使用页面证据无法证明的隐藏高精度。
_Avoid_: 二进制浮点比较、隐藏接口精度、只存规范值丢失原文、显示未变却触发截图

**口碑量数量语义**：
平台可见口碑量的数值性质：确定数量为 `exact`，按万或亿显示的可见舍入值为 `rounded`，带“+”或“以上”的下限为 `lower_bound`；规范值统一换算为“条”并保留原文，只有前后语义相同才比较，语义变化记为 `not_comparable`。

**口碑评价篇数**：
懂车帝评分页“全部评分”列表对应的评价文章总数。正式巡检从同一车型评分页 `__NEXT_DATA__.props.pageProps.reviewListData.total_count` 读取精确整数，与“共N人评价”的口碑量分开保存和比较，不访问车型圈子页。历史批次不回填当前值，首个新批次显示“暂无基线”。
_Avoid_: 使用圈子帖子总量代替评价文章数、使用评价人数代替文章数、用当前总量回填历史批次

**差评率**：
使用现有口碑平台车型ID作为APP接口 `series_id`，读取优点数与缺点数后按 `缺点数 ÷（优点数 + 缺点数）` 取整数百分比形成的独立指标；正式巡检默认采集并保存百分比、两个原始计数和接口来源URL，不增加用户配置。差评率下降为改善、上升为下降；历史批次显示“历史未采集”，首个新批次不反推历史值。
_Avoid_: 用低星评分占比替代差评率、只保存百分比丢弃分子分母、要求用户填写第二套车型ID或APP指纹

**口碑巡检汇报**：
基于一个口碑巡检批次冻结数据生成的不可变当日结果摘要；成功批次生成正常汇报，部分成功生成明确标记数据不完整的汇报，全部失败只形成失败记录，后续补跑不自动改写已生成内容。
_Avoid_: 动态查询报告、用旧值补齐、补跑后静默改写已发汇报、自动外发消息

**系统内口碑汇报**：
在口碑巡检批次详情中生成、查看、复制和下载的口碑巡检汇报；当前版本不拥有外部接收人、发送渠道、发送重试或送达状态。
_Avoid_: 企业微信通知、邮件、短信、自动发送给联系人

**口碑巡检汇报下载物**：
同一批次分别提供的 UTF-8 TXT 汇报正文和固定版式 XLSX 数据证据文件；TXT与页面一键复制内容一致，XLSX承载车型平台指标和备注单元格中的指标区域证据。
_Avoid_: DOCX或PDF重复排版、复制文字与TXT内容不一致、默认打成ZIP汇报包

**口碑巡检派生产物状态**：
汇报正文、TXT、XLSX和页面证据ZIP各自独立的生成状态；单个产物失败不改写巡检状态或删除其他成功产物，失败重建只读取冻结输入并按生成器版本形成可追溯文件，不重新访问平台。
_Avoid_: 文件失败改成巡检失败、重建时重新采集、晚到补跑自动改写成功文件、覆盖旧成功版本

**口碑派生产物首次成功发布版**：
一个历史批次的TXT、XLSX或证据ZIP类型中第一个成功生成并作为默认下载的版本；后续生成器升级产生的重建版保留独立版本信息并只由用户显式选择，不静默替换发布版或改变当日汇报快照。
_Avoid_: 默认下载自动追随最新版、允许随意重设默认、重建文件改写页面正文、相同输入重试制造新版本

**口碑巡检固定表格**：
由系统按口碑巡检批次冻结车型和平台范围生成的不可变XLSX成果，一行对应一个车型，平台分组包含口碑分、排名、口碑量、口碑评价篇数和差评率，备注嵌入该批次全量指标区域证据；它不使用用户上传模板或帖子导出标签。
_Avoid_: 帖子XLSX模板、用户上传口碑模板、预留未接入平台空列、重新查询平台导出

**口碑证据预览图**：
XLSX导出时把一个车型的1至3个平台指标区域证据按平台顺序排成的单张展示图；它只锚定在该车型备注列的一个单元格，引用原证据身份并保存自身校验值，不增加逻辑证据数量。
_Avoid_: 一个单元格叠放多张浮动图片、替代独立平台证据、再次裁切指标内容

**延迟口碑巡检汇报**：
计划汇报时刻到达时巡检尚未终态，先保持等待并在批次终态后生成的口碑巡检汇报；它记录计划时间与实际生成时间，不读取运行中数据制作半成品。
_Avoid_: 强行截断巡检、运行中半成品汇报、巡检状态与汇报状态混用、终态后继续等待固定时刻

**口碑变化汇报项**：
一个车型平台组合中至少一个口碑分、排名、口碑量、口碑评价篇数或差评率相对冻结前日基线发生变化时形成的汇报条目；正文只描述实际变化指标，并可关联该项已经按全量规则保存的同期口碑指标页面证据。
_Avoid_: 全量无变化车型清单、把有截图误解为必须进入变化正文、把异常缺失混入正常变化

**口碑汇报平台分组**：
口碑巡检汇报中按批次冻结平台顺序始终保留的实际平台章节。章节有“存在变化”“可比较但无变化”“无可比较结果”三种结果语义，异常与缺失独立列示；只有全部平台均可比较、均无变化且没有异常时，汇报顶部才可以概括为“今日无口碑指标变化”。
_Avoid_: 省略零变化平台、把全量异常写成无变化、用全局无变化提示掩盖异常

**月末巡检汇报**：
月末巡检批次按与日常巡检相同的前日变化规则生成的口碑巡检汇报；它额外标识月末巡检和全量页面证据完成情况，但不包含未定义的整月趋势、月均值或月初月末比较。
_Avoid_: 月报统计、自动推导月度趋势、用月初替代前日基线

**提取链接**：
提取批次中保存的一条帖子链接快照及平台在提取时提供的关联字段。
_Avoid_: 持续跟踪帖子

**舆情判定对象**：
一个 ThreadSnap 实例内统一配置、可版本化的最小业务边界，由目标品牌、重点产品及选填补充说明组成；常见别名、相关服务和具体语境由模型结合帖子判断，每次 AI 舆情反馈都明确引用当时版本。
_Avoid_: 人工穷举实体词典、后端关键词命中、按来源分别配置、提示词中的临时称谓

**AI 舆情反馈**：
在线多模态模型针对一条帖子原始快照返回的对象相关性、情感倾向、负面类型、模态覆盖和依据；它表达模型本次实际反馈，不等同于客观事实或人工确认结论。
_Avoid_: 智能体会话、事实裁决、准确率承诺

**有效舆情结论**：
当前帖子快照用于业务展示的 `negative`、`non_negative` 或 `unrelated` 舆情结果；有人工修正时采用人工结果，否则只有模态处理完整且结构有效的 AI 舆情反馈可以生效。
_Avoid_: 部分分析自动生效、覆盖原始 AI 返回

**结论来源**：
有效舆情结论的可追溯来源，只能是当前人工修正、继承人工判定或完整 AI 反馈；它作为舆情结果的附属标签展示，不是独立业务结论。
_Avoid_: 单独结果列、把部分 AI 反馈标记为有效来源

**人工判定记忆**：
人工修正后按平台帖子身份和内容指纹保存的判定，可供当前快照及内容实质未变化的未来快照继承，不回写其他历史批次。
_Avoid_: 强制逐条复核、按 URL 字符串直接继承、改写历史快照

**模态覆盖**：
一次 AI 舆情反馈对正文、图片、视频画面和视频音频的实际处理状态及对应依据；帖子不存在的模态不构成缺失，存在但未取得或无法理解的必要模态会使结果保持部分完成。
_Avoid_: 请求成功即视为全部理解、仅以文字代替视频判断

**主评论**：
直接回复帖子正文的一级评论。第一版每个帖子最多提取十条主评论，不采集主评论下面的楼中楼回复。
主评论是帖子快照的附属内容；帖子身份和正文或媒体有效时，评论数量、缺失或分页终止状态不改变帖子结果的正常入库与展示。
_Avoid_: 主楼、全部评论、楼中楼

**批次导出**：
从数据库中已经持久化的提取批次快照生成表格文件。导出过程不重新访问目标平台，也不改变原批次数据。
_Avoid_: 实时导出、重新采集导出

**平台会话**：
服务器受控浏览器从平台官方页面导出的 Cookie、LocalStorage、浏览器 Profile 或等价 Session 状态。账号登录会话的“已保存”只表示系统取得并持久化了结构有效的 Session；它不判断当前页面是否已登录，也不同时证明圈子、帖子、评论、点赞或其他采集端点当前可达。ThreadSnap 不接收、不保存目标平台账号密码；Session 本身属于敏感凭证，必须加密持久化且不得通过接口回显。
_Avoid_: 把 Session 已保存写成采集验证通过、系统用户、平台密码字段、普通外链登录、Cookie 回显

**采集访问门禁**：
真实来源或帖子任务在自己的 URL 上执行的平台响应、身份、正文或媒体及受保护字段检查。Session 保存操作不执行采集访问门禁；保存后的任务在原 URL 上通过门禁才形成有效结果，登录失效、访问验证、限流和内容错误分别使用自身状态处理。
_Avoid_: 在保存 Session 时访问圈子首帖、用首页登录外观宣称采集成功、隐藏真实平台控制

**访问会话**：
不要求也不证明账号身份，只通过平台目标页面访问检查的浏览器状态。该类型保留为平台能力词汇，但当前懂车帝、汽车之家和易车都使用账号登录会话；不得把历史访问会话的 `available` 沿用为易车账号已登录。
_Avoid_: 登录认证、账号有效、匿名访问一定足够、把 `available` 翻译为已登录

**受认证保护的帖子指标**：
平台在匿名页面显示占位值、而登录后才显示真实值的帖子字段。只有同一详情页同时证明有效登录身份和字段值时才能保存；匿名占位零值既不是真实零值，也不等同于平台没有该字段。汽车之家帖子点赞数属于此类指标。
_Avoid_: 把匿名占位零值保存为真实零、把解析失败静默改为空值、人工补填历史快照

**平台会话自动刷新**：
采集识别到账号身份异常或访问会话异常后，先使用既有加密浏览器 Profile 执行一次有界自动刷新；浏览器离开已知登录入口并导出结构有效的 Session 后保存，再从触发 URL 继续由采集访问门禁判断。自动刷新失败并使批次进入 `waiting_for_auth` 后，“处理会话”入口直接创建不继承旧状态的全新登录环境或访问环境，不再先展示已被采集门禁判定失效的旧 Profile；平台配置中的主动处理仍默认复用既有 Profile，并保留手动切换全新环境的入口。它不是保存密码后的自动重新登录。
_Avoid_: 把访问异常等同账号退出、无限重登、验证码自动处理、换 Session 固定扩容

**平台交互会话处理**：
账号登录会话需要操作官方登录表单、扫码、短信、验证码或滑块，或访问会话需要重新初始化时，浏览器进程、Profile 和 Session 位于服务器；第一版客户从受控内网客户机的普通浏览器，通过基础测试界面的临时嵌入式入口操作服务器浏览器并保存 Session。该入口不访问圈子或帖子做采集检查；客户机本地浏览器里的状态不作为服务器采集 Session。
_Avoid_: 把全部会话处理都称为登录认证、前端收集平台密码、直接暴露 CDP/VNC、复制个人浏览器 Cookie

**等待平台会话**：
批次因账号登录会话失效或访问会话异常而进入的可恢复暂停状态，代码值继续为 `waiting_for_auth`。系统持久化当前索引、触发 URL 和待处理队列后释放 Worker；平台 Session 保存后自动续跑同一批次，并在原 URL 重新执行采集访问门禁，不要求人工创建替代批次。
_Avoid_: 把访问会话异常一律写成未登录、把会话要求直接记为失败终态、会话更新后新建替代批次、交互窗口关闭时丢失检查点

**提取功能服务**：
持续拥有提取配置、定时规则、提取批次、提取链接、平台 Session、采集器、持久化和任务调度的独立后端；第一版同时提供前端页面 API 和稳定集成 API，后续客户现有后端直接复用集成 API。
_Avoid_: Python模块、爬虫脚本

**双接口提取后端**：
第一版自建前端通过页面 API 使用提取后端；后续客户现有后端通过调用方无关的集成 API 使用同一提取后端。两套控制器共用应用层和领域实现，不复制采集、调度、Session、持久化或状态机。
_Avoid_: 两套采集后端、前端直连集成接口、临时代理服务

**平台采集配置**：
一个平台共享的运行与接入配置，包含接入状态、平台内部总并发、平台启用状态和平台 Session；它不保存自动提取数量、圈子链接、车型或定时时间。新平台接入后默认停用，只有启用计划节点所引用规则均已填写该平台数量时才可启用。
_Avoid_: 自动提取规则、圈子配置、平台独立定时器、单圈并发

**自动提取规则**：
由用户命名、可被多个每周或循环计划节点复用的一组定时提取业务参数与明确来源范围；用户按平台勾选具体平台圈子来源，只为已选来源涉及的平台配置统一每来源有效结果目标数，并明确选择是否执行 AI 舆情分析、是否生成圈子页面截图。规则保存稳定来源配置 ID，不以“平台全选”动态包含未来新增来源；规则 ID、版本和更新时间由系统维护，范围、数量或两个输出开关变化都会产生同一逻辑规则的新版本，已经创建的批次保留触发时版本与来源快照。规则不保存星期、时刻、时间段、间隔、平台运行参数、Session 或手动提取参数。
_Avoid_: 动态全平台范围、每周计划节点、循环计划节点、平台采集配置、手动提取参数

**每周计划节点**：
由一个全局调度源维护的一条每周循环触发定义，包含一个或多个星期、24 小时制 `HH:mm:ss` 时刻、启用状态和一条或多条自动提取规则引用；节点本身不重复保存规则参数。节点到点后冻结全部规则版本并合并为一个批次，重复圈子只创建一个任务且采用这些规则中最大的有效结果目标数。启用的每周节点按逐日展开后的“星期 + 时刻”在每周类型内全局唯一；它可以与循环节点同秒触发，两者分别创建定时批次和循环批次并进入同一平台 FIFO。停用的同类型节点可以暂存重叠但启用时必须重新校验。停机期间错过的节点不补跑，已经持久化入队的任务必须恢复。
_Avoid_: 自动提取规则副本、平台定时器、圈子定时器、自定义 Cron

**来源名称**：
用户为一个或多个平台圈子来源填写的业务展示名称，例如“风云A9最新回复”或“风云A9最新发布”。它用于规则选择、批次提取范围和结果来源展示，不参与来源唯一性或 XLSX 字段标签寻址；历史接口和存储中的 `vehicle`/`vehicle_name` 仅作为兼容字段名保留。
_Avoid_: 车型、平台返回圈子名称、稳定来源 ID

**平台圈子来源**：
由稳定平台代码、平台圈子 ID、版块和列表顺序共同确定的采集来源，可填写来源名称并通过全局开关决定是否具备自动提取资格；具体自动提取规则再从具备或待恢复资格的来源中明确选择范围。同一个懂车帝圈子可以分别保存动态版块的“最新回复”和“最新发布”两个来源，两者使用不同规范化链接、来源配置 ID、验证结果和批次快照。
_Avoid_: 来源名称、帖子 URL、仅用平台圈子 ID 判重

**圈子首次验证**：
一个配置圈子从未成功验证到第一次成功验证的单次状态转换。首次成功后系统自动开启该圈子的全局自动参与资格并永久记录首次验证时间；此后重新验证只刷新可用性、名称和最近验证结果，不改变用户手动设置的自动参与开关。
_Avoid_: 每次验证、当前为未验证、重新认证后自动启用

**手动圈子历史**：
用户手动输入并通过验证的平台圈子记录，供后续手动提取再次选择，永久保留到用户删除且从不自动加入定时提取范围。
_Avoid_: 自动提取圈子、临时不可导出数据

**圈子提取任务**：
一个提取批次中针对某个平台圈子的执行单元；同一平台内严格按批次创建顺序排队，多个圈子可在平台总并发上限内推进。
_Avoid_: 独立定时任务、独立提取批次

**圈子页面原始证据**：
一个开启圈子页面截图的圈子提取任务在发现帖子时，对实际经过的一个或多个平台物理分页同步冻结的原始截图；它不注入改变页面像素的样式，保持平台原貌，并在实际入库帖子卡片的文字、媒体和布局完整稳定后作为关联圈子截图成果的可追溯来源。
_Avoid_: 最终成果、采集完成后补拍、重新访问平台生成、把未完成加载的卡片记为完整证据

**圈子页面清单**：
一个圈子提取任务在冻结圈子页面原始证据时同步确定的有序帖子集合；帖子详情采集与关联圈子截图成果共同以该清单为来源，页面重新排序不改变已经冻结的集合。
_Avoid_: 按时间接近推定一致、截图与采集各自发现列表、采集后重新生成清单

**固定候选清单**：
一个圈子提取任务在详情访问前，从所选来源的冻结顺序中取得的前 N 个去重帖子身份和规范 URL，其中 N 为该任务配置数量；网络访问失败只持久重试原来源或原帖子 URL，不把列表中更靠后的帖子替换进本任务。开启页面截图时，它必须可追溯到同次圈子页面清单和原始证据中的相同卡片与顺序。
_Avoid_: 按最终成功数向后补位、访问失败后重新发现下一帖、截图与结果对应不同候选、把重试次数当候选数量

**证据化帖子**：
一个实际入库帖子在同次圈子页面清单和完整原始证据中具有可追溯卡片的状态；只有证据化帖子进入关联圈子截图成果，功能启用前的历史帖子不通过事后访问平台补成该状态。
_Avoid_: 只有数据库快照的历史帖子、采集完成后补拍、按相同帖子 ID 推定已有页面证据

**关联圈子成果组**：
一个原批次及其全部补提批次中的同一实际执行来源共享的稳定成果归属；删除任一贡献批次只移除该批次的内容并按剩余贡献重建当前成果，最后一个贡献批次删除后成果组才随之清理。
_Avoid_: 绑定某一个批次所有、删除原批次级联删除补提数据、跨来源合并

**关联圈子截图成果**：
一个关联圈子成果组当前拥有的唯一逻辑截图成果，按关联结果首次引用原始页面证据的稳定顺序保留完整物理分页，并只在有效负面帖子的原卡片边界内叠加红框；一张或多张完整页面文件共同承载同一个逻辑成果。它明确标识跨批次派生性质以及每张页面、每个帖子卡片的来源批次和采集时间。批次开启 AI 时，当前关联结果全部形成有效舆情结论后才成为最终成果；批次关闭 AI 时不等待模型，直接形成无红框原图成果，后续人工有效结论可触发新版本。零条负面时仍生成对应完整原页的无红框成果，平台明确返回空列表时生成零结果成果，错误导致零条时不生成最终成果；同圈子的不同列表顺序分别形成成果，未被批次执行或关闭截图的配置来源不生成成果。
_Avoid_: 裁取并重排帖子卡片、增加成果标题栏、每个补提批次各自一份逻辑成果、逐帖截图、为未执行来源补拍、截图 OCR 后另建舆情结论

**截图成果分片**：
关联圈子截图成果中的一个完整原始物理分页副本；它保持原图尺寸、布局和除负面红框外的页面像素，只承担同一个逻辑成果的存储与连续展示，不增加成果数量，也不把多个页面重新拼成长图。
_Avoid_: 独立截图成果、裁切原始页面、把多个页面重新合成超长图、降低帖子采集上限

**关联圈子截图成果状态**：
圈子页面原始证据和关联圈子截图成果自身的处理状态，与帖子提取状态和舆情分析状态分别表达；成果异常不改变已经确认的帖子提取结果，也不触发帖子失败项补提。
_Avoid_: 提取批次状态、舆情分析状态、截图失败等同帖子失败

**关联圈子截图成果版本**：
同一来源的关联结果因补提、删除或有效舆情结论变化而生成的新一版关联圈子截图成果；前端只把最新版作为当前成果，历史版本连同生成时间、结果数量、负面数量和文件校验值保留用于追溯。
_Avoid_: 覆盖历史成果、同时展示多个当前成果、修改原始分页证据

**手动补提**：
终态提取批次由用户发起的后续提取，只包含原批次中仍缺失的圈子并创建新的手动批次；前端把各批次剩余的最新成功结果关联汇总，但不改写原始快照。该词只作为内部领域简称，用户界面的操作统一称为“重新提取失败项”。
_Avoid_: 自动重试、覆盖原批次、物理合并快照

**导出模板标签**：
XLSX 模板中寻址导出数据的稳定英文标识，格式为 `s.<source_key>.<field>`；`source_key` 是来源持久化的 10 位全局唯一导出键，不含平台信息。所有平台共用同一字段后缀注册表；标签不使用中文名称、平台代码、来源 UUID 或数据库物理字段名。
_Avoid_: 工作表名称推断、数据库列名、中文字段标签

**Linux PoC 测试包**：
候选方案完成本地访问冒烟和批量预筛后生成的最简 `tar.gz` 发布包，由项目负责人手动部署到最终 Linux 主机，并从干净目录完成安装、启动、访问验证和目标吞吐测试。测试包包含面向目标 Linux 环境生成的离线应用依赖及锁文件，但不包含开发机虚拟环境、凭证或机器状态。
_Avoid_: Windows 环境复制、正式生产安装包、Docker 镜像

**采集框架 PoC**：
正式功能开发前的技术选型测试。当前只验证候选框架能否在一小时内访问 2000 个不同真实帖子 URL，识别对应帖子与风控响应，并产生可复核的逐 URL 结果；不以此代替圈子发现、正文与主评论、数据库、前端和导出的第一版功能验收。
_Avoid_: 第一版完工验收、仅 HTTP 200 即成功

**PoC 本地产物目录**：
当前开发电脑统一使用 `artifacts/poc/` 保存不进入 Git 的 PoC 产物；甲方真实清单放在 `inputs/`，测试包放在 `packages/`，Linux 复制回来的各候选各轮结果放在 `results/<candidate>/<round-id>/`，不把测试文件散落到仓库其他路径。
_Avoid_: 仓库根目录 results、随意临时目录、原始结果提交 Git

**风控结果门禁**：
不单独指定或验收某一种所谓“绕过”技术，而是按真实结果判定：限流、验证码、挑战页、登录失效或其他平台控制只要导致任一 URL 未在一小时窗口内产生通过统一校验的访问证明，该轮 PoC 即失败；自动恢复并在窗口内完成时仍可计为通过。
_Avoid_: 排队数量、挑战页成功、永久稳定承诺
