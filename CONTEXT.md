# ThreadSnap 领域语境

ThreadSnap 领域描述按配置执行论坛链接提取，并将每次执行结果保存为独立快照的业务概念。

## Language

**提取配置**：
定义一次手动或定时链接提取所需的平台、圈子、版块、数量上限和定时规则。
_Avoid_: 监控配置

**内部验证版**：
仅在受控内部环境中使用，用于验证懂车帝平台采集与基础提取闭环的第一阶段交付。
_Avoid_: 公网版、正式版

**正式版**：
内部验证版通过后面向公网使用的后续阶段，包含身份认证和角色权限控制。
_Avoid_: 测试版、内部验证版

**提取批次**：
一次手动或定时提取执行及其结果集合；不同批次彼此独立，不覆盖或合并。
_Avoid_: 监控记录、同步记录

**提取链接**：
提取批次中保存的一条帖子链接快照及平台在提取时提供的关联字段。
_Avoid_: 持续跟踪帖子

**主评论**：
直接回复帖子正文的一级评论。第一版每个帖子最多提取十条主评论，不采集主评论下面的楼中楼回复。
_Avoid_: 主楼、全部评论、楼中楼

**批次导出**：
从数据库中已经持久化的提取批次快照生成表格文件。导出过程不重新访问目标平台，也不改变原批次数据。
_Avoid_: 实时导出、重新采集导出

**平台凭证**：
仅在未登录模式不能满足提取要求时，为访问目标平台而保存的账号、密码、Cookie 或浏览器会话状态。内部验证版允许通过配置页面录入并在本地数据库明文保存密码，但密码不能通过查询接口回显；正式公网版本必须迁移为加密存储。
_Avoid_: 系统用户、密码回显、长期明文存储

**提取功能服务**：
持续拥有提取配置、定时规则、提取批次和提取链接，并向现有后端提供内部接口的独立功能边界。
_Avoid_: Python模块、爬虫脚本

**Linux PoC 测试包**：
候选方案完成本地功能和批量预筛后生成的最简 `tar.gz` 发布包，由项目负责人手动部署到最终 Linux 主机，并从干净目录完成安装、启动、样本验证和目标吞吐测试。测试包包含面向目标 Linux 环境生成的离线应用依赖及锁文件，但不包含开发机虚拟环境、凭证或机器状态。
_Avoid_: Windows 环境复制、正式生产安装包、Docker 镜像

**PoC 本地产物目录**：
当前开发电脑统一使用 `artifacts/poc/` 保存不进入 Git 的 PoC 产物；甲方真实清单放在 `inputs/`，测试包放在 `packages/`，Linux 复制回来的各候选各轮结果放在 `results/<candidate>/<round-id>/`，不把测试文件散落到仓库其他路径。
_Avoid_: 仓库根目录 results、随意临时目录、原始结果提交 Git

**风控结果门禁**：
不单独指定或验收某一种所谓“绕过”技术，而是按真实结果判定：限流、验证码、挑战页、登录失效或其他平台控制只要导致任一 URL 未在一小时窗口内产生完整有效结果，该轮 PoC 即失败；自动恢复并在窗口内完整完成时仍可计为通过。
_Avoid_: 排队数量、挑战页成功、永久稳定承诺
