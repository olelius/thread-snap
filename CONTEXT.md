# ThreadSnap 领域语境

ThreadSnap 领域描述从圈子版块自动发现或从已知帖子 URL 清单导入链接，并将每次执行结果保存为独立快照的业务概念。

## Language

**提取配置**：
定义一次手动或定时链接提取所需的平台、圈子、版块、数量上限和定时规则。
_Avoid_: 监控配置

**圈子发现提取**：
按提取配置访问指定圈子和版块，翻页发现帖子 URL，再交给统一详情采集流程处理。
_Avoid_: URL 清单导入、持续监控

**URL 清单提取**：
导入一组已知帖子 URL 直接创建手动提取批次，不经过圈子列表发现，但与圈子发现提取复用同一平台详情采集流程。
_Avoid_: 圈子 URL、性能测试结果文件

**内部验证版**：
仅在受控内部环境中使用，用于验证懂车帝平台采集与基础提取闭环的第一阶段交付。
_Avoid_: 公网版、正式版

**正式版**：
内部验证版通过后面向公网使用的后续阶段，包含身份认证和角色权限控制。
_Avoid_: 测试版、内部验证版

**提取批次**：
一次手动或定时提取执行及其结果集合；不同批次彼此独立，不覆盖或合并。
_Avoid_: 监控记录、同步记录

**提取链接**：
提取批次中保存的一条帖子链接快照及平台在提取时提供的关联字段。
_Avoid_: 持续跟踪帖子

**主评论**：
直接回复帖子正文的一级评论。第一版每个帖子最多提取十条主评论，不采集主评论下面的楼中楼回复。
_Avoid_: 主楼、全部评论、楼中楼

**批次导出**：
从数据库中已经持久化的提取批次快照生成表格文件。导出过程不重新访问目标平台，也不改变原批次数据。
_Avoid_: 实时导出、重新采集导出

**平台凭证**：
仅在未登录模式不能满足提取要求时，由用户在平台官方登录页面完成认证后形成的 Cookie、LocalStorage、浏览器 Profile 或等价 Session 状态。ThreadSnap 不接收、不保存目标平台账号密码；Session 本身属于敏感凭证，必须加密持久化且不得通过接口回显。
_Avoid_: 系统用户、平台密码字段、普通外链登录、Cookie 回显

**平台会话自动刷新**：
HTTP 采集识别到身份异常后，使用既有加密浏览器 Profile 打开官方页面；若平台仍认可该身份，则重新导出 Session、通过门禁并从触发 URL 继续。它不是保存密码后的自动重新登录。
_Avoid_: 无限重登、验证码自动处理、换 Session 固定扩容

**平台人工认证**：
官方页面出现登录表单、扫码、短信、验证码或滑块，或者自动刷新后的门禁仍失败时，浏览器进程、Profile 和 Session 位于服务器；客户从客户机通过临时、已鉴权远程交互入口操作服务器浏览器完成官方认证。客户机本地浏览器里的登录状态不作为服务器采集 Session。
_Avoid_: 前端收集平台密码、直接暴露 CDP/VNC、复制个人浏览器 Cookie

**等待平台认证**：
批次因平台要求人工认证而进入的可恢复暂停状态，代码值为 `waiting_for_auth`。系统持久化当前索引、触发 URL 和待处理队列后释放 Worker；认证完成且 Session 门禁通过后自动续跑同一批次，不要求人工重试。
_Avoid_: 把认证要求直接记为失败终态、认证后新建替代批次、认证窗口关闭时丢失检查点

**提取功能服务**：
持续拥有提取配置、定时规则、提取批次和提取链接，并向现有后端提供内部接口的独立功能边界。
_Avoid_: Python模块、爬虫脚本

**Linux PoC 测试包**：
候选方案完成本地访问冒烟和批量预筛后生成的最简 `tar.gz` 发布包，由项目负责人手动部署到最终 Linux 主机，并从干净目录完成安装、启动、访问验证和目标吞吐测试。测试包包含面向目标 Linux 环境生成的离线应用依赖及锁文件，但不包含开发机虚拟环境、凭证或机器状态。
_Avoid_: Windows 环境复制、正式生产安装包、Docker 镜像

**采集框架 PoC**：
正式功能开发前的技术选型测试。当前只验证候选框架能否在一小时内访问 2000 个不同真实帖子 URL，识别对应帖子与风控响应，并产生可复核的逐 URL 结果；不以此代替圈子发现、正文与主评论、数据库、前端和导出的第一版功能验收。
_Avoid_: 第一版完工验收、仅 HTTP 200 即成功

**PoC 本地产物目录**：
当前开发电脑统一使用 `artifacts/poc/` 保存不进入 Git 的 PoC 产物；甲方真实清单放在 `inputs/`，测试包放在 `packages/`，Linux 复制回来的各候选各轮结果放在 `results/<candidate>/<round-id>/`，不把测试文件散落到仓库其他路径。
_Avoid_: 仓库根目录 results、随意临时目录、原始结果提交 Git

**风控结果门禁**：
不单独指定或验收某一种所谓“绕过”技术，而是按真实结果判定：限流、验证码、挑战页、登录失效或其他平台控制只要导致任一 URL 未在一小时窗口内产生通过统一校验的访问证明，该轮 PoC 即失败；自动恢复并在窗口内完成时仍可计为通过。
_Avoid_: 排队数量、挑战页成功、永久稳定承诺
