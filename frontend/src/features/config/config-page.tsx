import { useEffect, useState } from 'react'
import { useBlocker, useNavigate, useSearch } from '@tanstack/react-router'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { ArchiveRestore, CalendarClock, CirclePlus, Copy, KeyRound, Loader2, Plus, Save, Trash2, Upload } from 'lucide-react'
import { toast } from 'sonner'
import { AuthDialog } from '@/features/auth/auth-dialog'
import { PageHeader } from '@/components/page-header'
import { StatusBadge } from '@/components/status-badge'
import { api, errorMessage, formatDate, queryString } from '@/lib/api'
import type { Circle, ExtractionPlan, Platform, SessionStatus, Template, Vehicle } from '@/lib/types'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from '@/components/ui/alert-dialog'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Separator } from '@/components/ui/separator'
import { Switch } from '@/components/ui/switch'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { Textarea } from '@/components/ui/textarea'

const tabValues = ['plan', 'platforms', 'circles', 'history', 'templates'] as const
type Tab = typeof tabValues[number]
const weekdays = ['一', '二', '三', '四', '五', '六', '日']

export function ConfigPage() {
  const raw = useSearch({ strict: false }) as { tab?: Tab }
  const navigate = useNavigate({ from: '/config' })
  const [planDirty, setPlanDirty] = useState(false)
  const [platformDirty, setPlatformDirty] = useState(false)
  const [circleDirty, setCircleDirty] = useState(false)
  const dirty = planDirty || platformDirty || circleDirty
  const blocker = useBlocker({
    shouldBlockFn: ({ current, next }) => dirty && current.pathname !== next.pathname,
    enableBeforeUnload: dirty,
    disabled: !dirty,
    withResolver: true,
  })
  const tab = tabValues.includes(raw.tab as Tab) ? raw.tab as Tab : 'plan'
  return (
    <div className='space-y-6'>
      <PageHeader title='配置管理' description='每项配置只在唯一归属页面编辑；跨页区域只展示摘要和跳转入口。' />
      {dirty && <Alert className='border-amber-500/30 bg-amber-500/5'><Save className='size-4' /><AlertTitle>存在尚未保存的修改</AlertTitle><AlertDescription>切换标签会保留暂存内容；离开或刷新页面前会提示。保存按钮只提交当前标签。</AlertDescription></Alert>}
      <Tabs value={tab} onValueChange={(value) => navigate({ to: '/config', search: { tab: value as Tab }, replace: true })}>
        <div className='overflow-x-auto'><TabsList className='h-11 min-w-max bg-muted/65 p-1'><TabsTrigger value='plan'>提取计划</TabsTrigger><TabsTrigger value='platforms'>平台配置</TabsTrigger><TabsTrigger value='circles'>车型与圈子</TabsTrigger><TabsTrigger value='history'>手动圈子历史</TabsTrigger><TabsTrigger value='templates'>导出模板</TabsTrigger></TabsList></div>
        <TabsContent forceMount value='plan' className='mt-5 data-[state=inactive]:hidden'><PlanPanel onDirtyChange={setPlanDirty} /></TabsContent>
        <TabsContent forceMount value='platforms' className='mt-5 data-[state=inactive]:hidden'><PlatformPanel onDirtyChange={setPlatformDirty} /></TabsContent>
        <TabsContent forceMount value='circles' className='mt-5 data-[state=inactive]:hidden'><CirclePanel onDirtyChange={setCircleDirty} /></TabsContent>
        <TabsContent forceMount value='history' className='mt-5 data-[state=inactive]:hidden'><HistoryPanel /></TabsContent>
        <TabsContent forceMount value='templates' className='mt-5 data-[state=inactive]:hidden'><TemplatePanel /></TabsContent>
      </Tabs>
      <AlertDialog open={blocker.status === 'blocked'}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>放弃尚未保存的修改？</AlertDialogTitle>
            <AlertDialogDescription>离开配置管理后，所有标签中尚未保存的内容都会被放弃。</AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel onClick={() => blocker.status === 'blocked' && blocker.reset()}>留在当前页面</AlertDialogCancel>
            <AlertDialogAction onClick={() => blocker.status === 'blocked' && blocker.proceed()}>放弃并离开</AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </div>
  )
}

function PlanPanel({ onDirtyChange }: { onDirtyChange: (dirty: boolean) => void }) {
  const client = useQueryClient()
  const query = useQuery({ queryKey: ['extraction-plan'], queryFn: () => api<ExtractionPlan>('/extraction-plan') })
  const platforms = useQuery({ queryKey: ['platforms'], queryFn: () => api<Platform[]>('/platforms') })
  const [draft, setDraft] = useState<ExtractionPlan>()
  useEffect(() => { if (query.data && !draft) setDraft(structuredClone(query.data)) }, [query.data, draft])
  function update(next: ExtractionPlan) { setDraft(next); onDirtyChange(true) }
  const save = useMutation({ mutationFn: () => api<ExtractionPlan>('/extraction-plan', { method: 'PUT', body: JSON.stringify({ revision: draft?.revision, rules: draft?.rules.map(({ id, name, platform_quantities }) => ({ id, name, platform_quantities })), nodes: draft?.nodes.map(({ id, weekdays, time, enabled, rule_id }) => ({ id, weekdays, time, enabled, rule_id })) }) }), onSuccess: (value) => { setDraft(structuredClone(value)); onDirtyChange(false); client.setQueryData(['extraction-plan'], value); toast.success('提取计划已保存') }, onError: (error) => toast.error('保存失败', { description: errorMessage(error) }) })
  if (!draft) return <Card><CardContent className='p-10 text-center text-sm text-muted-foreground'>正在加载提取计划…</CardContent></Card>
  const integrated = platforms.data?.filter((item) => item.adapter_status === 'available') ?? []
  return <div className='space-y-5'>
    <div className='flex flex-wrap items-center justify-between gap-3'><div><h2 className='text-lg font-semibold'>自动提取规则</h2><p className='text-sm text-muted-foreground'>规则只维护名称和每个平台的每圈有效结果目标数。</p></div><div className='flex gap-2'><Badge variant='outline'>计划版本 {draft.revision}</Badge><Button variant='outline' onClick={() => update({ ...draft, rules: [...draft.rules, { id: crypto.randomUUID(), name: `新规则 ${draft.rules.length + 1}`, version: 1, platform_quantities: Object.fromEntries(integrated.map((item) => [item.code, Math.min(30, item.quantity_range.max)])), archived: false, updated_at: new Date().toISOString() }] })}><Plus className='size-4' />新建规则</Button><Button disabled={save.isPending} onClick={() => save.mutate()}>{save.isPending ? <Loader2 className='size-4 animate-spin' /> : <Save className='size-4' />}保存当前标签</Button></div></div>
    <div className='grid gap-4 xl:grid-cols-2'>{draft.rules.map((rule) => { const referenced = draft.nodes.some((node) => node.rule_id === rule.id); return <Card key={rule.id} className='border-border/70 bg-card/88'><CardHeader className='pb-3'><div className='flex items-start gap-3'><div className='min-w-0 flex-1'><Label htmlFor={`rule-${rule.id}`}>规则名称</Label><Input id={`rule-${rule.id}`} className='mt-2' value={rule.name} onChange={(event) => update({ ...draft, rules: draft.rules.map((item) => item.id === rule.id ? { ...item, name: event.target.value } : item) })} /></div><Button variant='ghost' size='icon' disabled={referenced} onClick={() => update({ ...draft, rules: draft.rules.filter((item) => item.id !== rule.id) })} aria-label={referenced ? '规则仍被计划节点引用' : '删除规则'}><Trash2 className='size-4' /></Button></div><CardDescription>ID {rule.id.slice(0, 8)} · 当前版本 {rule.version}</CardDescription></CardHeader><CardContent className='space-y-3'>{integrated.map((platform) => <div key={platform.code} className='grid grid-cols-[1fr_150px] items-center gap-3 rounded-lg border p-3'><div><div className='text-sm font-medium'>{platform.display_name}</div><div className='text-xs text-muted-foreground'>有效结果目标数 · {platform.quantity_range.min}–{platform.quantity_range.max}</div></div><Input type='number' min={platform.quantity_range.min} max={platform.quantity_range.max} value={rule.platform_quantities[platform.code] ?? ''} onChange={(event) => update({ ...draft, rules: draft.rules.map((item) => item.id === rule.id ? { ...item, platform_quantities: { ...item.platform_quantities, [platform.code]: Number(event.target.value) } } : item) })} /></div>)}</CardContent></Card> })}</div>
    <Separator />
    <div className='flex flex-wrap items-center justify-between gap-3'><div><h2 className='text-lg font-semibold'>每周计划节点</h2><p className='text-sm text-muted-foreground'>选择星期与 24 小时制时分秒，每个节点引用一个规则。</p></div><Button variant='outline' disabled={!draft.rules.length} onClick={() => update({ ...draft, nodes: [...draft.nodes, { id: crypto.randomUUID(), weekdays: [0, 1, 2, 3, 4], time: '09:00:00', enabled: false, rule_id: draft.rules[0].id, updated_at: new Date().toISOString() }] })}><CirclePlus className='size-4' />新增节点</Button></div>
    <div className='space-y-3'>{draft.nodes.length ? draft.nodes.map((node) => <Card key={node.id} className='border-border/70'><CardContent className='grid gap-4 p-4 xl:grid-cols-[auto_1fr_170px_240px_auto] xl:items-center'><Switch checked={node.enabled} onCheckedChange={(enabled) => update({ ...draft, nodes: draft.nodes.map((item) => item.id === node.id ? { ...item, enabled } : item) })} aria-label='启用计划节点' /><div className='flex flex-wrap gap-1.5'>{weekdays.map((label, index) => <Button key={label} type='button' variant={node.weekdays.includes(index) ? 'default' : 'outline'} size='icon' className='size-8 rounded-full' onClick={() => update({ ...draft, nodes: draft.nodes.map((item) => item.id === node.id ? { ...item, weekdays: item.weekdays.includes(index) ? item.weekdays.filter((day) => day !== index) : [...item.weekdays, index].sort() } : item) })} aria-label={`星期${label}`}>{label}</Button>)}</div><Input type='time' step={1} value={node.time} onChange={(event) => update({ ...draft, nodes: draft.nodes.map((item) => item.id === node.id ? { ...item, time: event.target.value.length === 5 ? `${event.target.value}:00` : event.target.value } : item) })} /><Select value={node.rule_id} onValueChange={(rule_id) => update({ ...draft, nodes: draft.nodes.map((item) => item.id === node.id ? { ...item, rule_id } : item) })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{draft.rules.map((rule) => <SelectItem key={rule.id} value={rule.id}>{rule.name}</SelectItem>)}</SelectContent></Select><Button variant='ghost' size='icon' onClick={() => update({ ...draft, nodes: draft.nodes.filter((item) => item.id !== node.id) })} aria-label='删除计划节点'><Trash2 className='size-4' /></Button></CardContent></Card>) : <div className='rounded-xl border border-dashed p-10 text-center text-sm text-muted-foreground'><CalendarClock className='mx-auto mb-3 size-8 text-primary/60' />尚未配置每周计划节点。</div>}</div>
    {draft.archived_rules.length > 0 && <Card><CardHeader><CardTitle className='text-base'>已归档规则</CardTitle><CardDescription>历史批次仍保留规则版本快照，可按需恢复。</CardDescription></CardHeader><CardContent className='space-y-2'>{draft.archived_rules.map((rule) => <div key={rule.id} className='flex items-center justify-between rounded-lg border p-3'><div><div className='text-sm font-medium'>{rule.name}</div><div className='text-xs text-muted-foreground'>版本 {rule.version}</div></div><Button variant='outline' size='sm' onClick={async () => { try { const value = await api<ExtractionPlan>(`/extraction-rules/${rule.id}/restore`, { method: 'POST' }); setDraft(value); client.setQueryData(['extraction-plan'], value); toast.success('规则已恢复') } catch (error) { toast.error('恢复失败', { description: errorMessage(error) }) } }}><ArchiveRestore className='size-4' />恢复</Button></div>)}</CardContent></Card>}
  </div>
}

function PlatformPanel({ onDirtyChange }: { onDirtyChange: (dirty: boolean) => void }) {
  const client = useQueryClient(); const query = useQuery({ queryKey: ['platforms'], queryFn: () => api<Platform[]>('/platforms') }); const [draft, setDraft] = useState<Platform[]>(); const [auth, setAuth] = useState<Platform>()
  useEffect(() => { if (query.data && !draft) setDraft(structuredClone(query.data)) }, [query.data, draft])
  const save = useMutation({ mutationFn: async () => Promise.all((draft ?? []).map((item) => api<Platform>(`/platforms/${item.code}`, { method: 'PUT', body: JSON.stringify({ enabled: item.enabled, internal_concurrency: item.internal_concurrency }) }))), onSuccess: (items) => { setDraft(items); client.setQueryData(['platforms'], items); onDirtyChange(false); toast.success('平台配置已保存') }, onError: (error) => toast.error('保存失败', { description: errorMessage(error) }) })
  if (!draft) return <Card><CardContent className='p-10 text-center text-sm text-muted-foreground'>正在加载平台配置…</CardContent></Card>
  const update = (code: string, values: Partial<Platform>) => { setDraft(draft.map((item) => item.code === code ? { ...item, ...values } : item)); onDirtyChange(true) }
  return <div className='space-y-5'><div className='flex justify-end'><Button disabled={save.isPending} onClick={() => save.mutate()}><Save className='size-4' />保存当前标签</Button></div><div className='grid gap-4 xl:grid-cols-3'>{draft.map((platform) => <Card key={platform.code} className='border-border/70 bg-card/88'><CardHeader><div className='flex items-start justify-between'><div><CardTitle>{platform.display_name}</CardTitle><CardDescription className='mt-1'>{platform.adapter_status === 'available' ? `适配器 ${platform.adapter_version ?? '已接入'}` : '后续平台预留'}</CardDescription></div><StatusBadge value={platform.adapter_status === 'available' ? 'success' : 'unknown'} label={platform.adapter_status === 'available' ? '已接入' : '未接入'} /></div></CardHeader><CardContent className='space-y-5'><div className='flex items-center justify-between rounded-lg border p-3'><div><div className='text-sm font-medium'>平台启用</div><div className='text-xs text-muted-foreground'>决定是否允许新任务</div></div><Switch disabled={platform.adapter_status !== 'available'} checked={platform.enabled} onCheckedChange={(enabled) => update(platform.code, { enabled })} /></div><div><Label>平台内部并发</Label><Input className='mt-2' type='number' disabled={platform.adapter_status !== 'available'} min={platform.concurrency_range.min} max={platform.concurrency_range.max} value={platform.internal_concurrency} onChange={(event) => update(platform.code, { internal_concurrency: Number(event.target.value) })} /></div>{platform.adapter_status === 'available' && <SessionCard platform={platform} onAuth={() => setAuth(platform)} />}</CardContent></Card>)}</div><AuthDialog open={Boolean(auth)} onOpenChange={(open) => !open && setAuth(undefined)} platformCode={auth?.code} platformName={auth?.display_name} /></div>
}

function SessionCard({ platform, onAuth }: { platform: Platform; onAuth: () => void }) {
  const client = useQueryClient(); const session = useQuery({ queryKey: ['session', platform.code], queryFn: () => api<SessionStatus>(`/platforms/${platform.code}/session`) })
  const statusLabel = session.data ? ({ missing: '未认证', valid: '有效', invalid: '已失效' }[session.data.status] ?? session.data.status) : '读取中'
  return <div className='rounded-xl border bg-muted/20 p-3'><div className='flex items-start justify-between gap-2'><div><div className='text-sm font-medium'>Session</div><div className='mt-1 text-xs text-muted-foreground'>最近验证：{formatDate(session.data?.last_verified_at)}</div></div><StatusBadge value={session.data?.status === 'valid' ? 'success' : 'unknown'} label={statusLabel} /></div><div className='mt-3 flex gap-2'><Button size='sm' variant='outline' onClick={onAuth}><KeyRound className='size-4' />去认证</Button><Button size='sm' variant='ghost' onClick={async () => { try { await api(`/platforms/${platform.code}/session`, { method: 'DELETE' }); client.invalidateQueries({ queryKey: ['session', platform.code] }); toast.success('平台会话已清除') } catch (error) { toast.error('清除失败', { description: errorMessage(error) }) } }}>清除</Button></div></div>
}

function CirclePanel({ onDirtyChange }: { onDirtyChange: (dirty: boolean) => void }) {
  const client = useQueryClient(); const query = useQuery({ queryKey: ['vehicles'], queryFn: () => api<Vehicle[]>('/vehicles') }); const [rows, setRows] = useState<Circle[]>()
  useEffect(() => { if (query.data && !rows) setRows(query.data.flatMap((vehicle) => vehicle.circles.map((circle) => ({ ...circle, vehicle_id: vehicle.id, vehicle_name: vehicle.name })))) }, [query.data, rows])
  if (!rows) return <Card><CardContent className='p-10 text-center text-sm text-muted-foreground'>正在加载车型与圈子…</CardContent></Card>
  const update = (index: number, values: Partial<Circle>) => { setRows(rows.map((item, rowIndex) => rowIndex === index ? { ...item, ...values } : item)); onDirtyChange(true) }
  const save = async () => { try { await api('/circles/batch', { method: 'PUT', body: JSON.stringify({ rows: rows.map((item) => ({ id: item.id || undefined, platform_code: item.platform_code || 'dongchedi', url: item.url, vehicle_id: item.vehicle_id || undefined, vehicle_name: item.vehicle_name || undefined, auto_enabled: item.auto_enabled, section: item.section || 'dynamic' })) }) }); onDirtyChange(false); client.invalidateQueries({ queryKey: ['vehicles'] }); toast.success('车型与圈子已保存') } catch (error) { toast.error('保存失败', { description: errorMessage(error) }) } }
  return <div className='space-y-4'><div className='flex flex-wrap items-center justify-between gap-3'><div><h2 className='text-lg font-semibold'>车型与圈子来源</h2><p className='text-sm text-muted-foreground'>这里唯一维护圈子来源、验证状态与自动参与资格。</p></div><div className='flex gap-2'><Button variant='outline' onClick={() => { setRows([...rows, { id: '', platform_code: 'dongchedi', external_id: '', url: '', vehicle_name: '未分组', auto_enabled: false, section: 'dynamic', validation_status: 'unverified' }]); onDirtyChange(true) }}><Plus className='size-4' />新增圈子</Button><Button onClick={save}><Save className='size-4' />保存当前标签</Button></div></div><div className='overflow-x-auto rounded-xl border bg-card/90'><Table className='min-w-[950px]'><TableHeader><TableRow><TableHead>车型</TableHead><TableHead>圈子 URL</TableHead><TableHead>名称</TableHead><TableHead>验证状态</TableHead><TableHead>自动参与</TableHead><TableHead className='text-right'>操作</TableHead></TableRow></TableHeader><TableBody>{rows.map((row, index) => <TableRow key={row.id || `new-${index}`}><TableCell><Input value={row.vehicle_name ?? ''} onChange={(event) => update(index, { vehicle_name: event.target.value, vehicle_id: undefined })} placeholder='车型名称' /></TableCell><TableCell><Input value={row.url} onChange={(event) => update(index, { url: event.target.value })} placeholder='圈子 URL' /></TableCell><TableCell>{row.name || (row.id ? '等待验证' : '保存后验证')}</TableCell><TableCell><StatusBadge value={row.validation_status === 'verified' ? 'success' : row.validation_status === 'failed' ? 'failed' : 'unknown'} label={{ verified: '已验证', failed: '验证失败', unverified: '未验证' }[row.validation_status] ?? row.validation_status} /></TableCell><TableCell><Switch checked={row.auto_enabled} disabled={row.validation_status !== 'verified'} onCheckedChange={(auto_enabled) => update(index, { auto_enabled })} /></TableCell><TableCell><div className='flex justify-end gap-1'>{row.id && <Button variant='outline' size='sm' onClick={async () => { try { await api(`/circles/${row.id}/validate`, { method: 'POST' }); toast.success('圈子验证已进入队列'); setTimeout(() => client.invalidateQueries({ queryKey: ['vehicles'] }), 1500) } catch (error) { toast.error('验证提交失败', { description: errorMessage(error) }) } }}>验证</Button>}<Button variant='ghost' size='icon' onClick={() => { setRows(rows.filter((_, rowIndex) => rowIndex !== index)); onDirtyChange(true) }} aria-label='删除圈子'><Trash2 className='size-4' /></Button></div></TableCell></TableRow>)}</TableBody></Table></div></div>
}

function HistoryPanel() {
  const client = useQueryClient(); const query = useQuery({ queryKey: ['manual-history'], queryFn: () => api<Circle[]>('/manual-circle-history') })
  async function remove(id?: string) { try { await api(`/manual-circle-history${id ? `/${id}` : ''}`, { method: 'DELETE' }); client.invalidateQueries({ queryKey: ['manual-history'] }); toast.success(id ? '历史记录已删除' : '手动圈子历史已清空') } catch (error) { toast.error('删除失败', { description: errorMessage(error) }) } }
  return <Card><CardHeader className='flex-row items-center justify-between'><div><CardTitle>手动圈子历史</CardTitle><CardDescription>只保存曾用于手动提取的临时圈子，不影响自动计划。</CardDescription></div><AlertDialog><AlertDialogTrigger asChild><Button variant='outline' disabled={!query.data?.length}>清空历史</Button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>清空全部手动圈子历史？</AlertDialogTitle><AlertDialogDescription>历史快捷入口将被移除，已经形成的批次快照保持不变。</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>取消</AlertDialogCancel><AlertDialogAction onClick={() => remove()}>确认清空</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></CardHeader><CardContent><div className='overflow-x-auto rounded-xl border'><Table><TableHeader><TableRow><TableHead>圈子</TableHead><TableHead>平台</TableHead><TableHead>最近使用</TableHead><TableHead className='text-right'>操作</TableHead></TableRow></TableHeader><TableBody>{query.data?.length ? query.data.map((circle) => <TableRow key={circle.id}><TableCell><div className='font-medium'>{circle.name || circle.external_id}</div><div className='max-w-lg truncate text-xs text-muted-foreground'>{circle.url}</div></TableCell><TableCell>{circle.platform_code === 'dongchedi' ? '懂车帝' : circle.platform_code}</TableCell><TableCell>{formatDate((circle as Circle & { last_used_at?: string }).last_used_at)}</TableCell><TableCell className='text-right'><Button variant='ghost' size='icon' onClick={() => remove(circle.id)} aria-label='删除历史记录'><Trash2 className='size-4' /></Button></TableCell></TableRow>) : <TableRow><TableCell colSpan={4} className='h-40 text-center text-muted-foreground'>暂无手动圈子历史。</TableCell></TableRow>}</TableBody></Table></div></CardContent></Card>
}

function TemplatePanel() {
  const client = useQueryClient()
  const query = useQuery({ queryKey: ['templates'], queryFn: () => api<Template[]>('/templates') })
  const vehicles = useQuery({ queryKey: ['vehicles'], queryFn: () => api<Vehicle[]>('/vehicles') })
  const [name, setName] = useState('')
  const [file, setFile] = useState<File>()
  const [circleKey, setCircleKey] = useState('')
  const [manualCopy, setManualCopy] = useState<string>()
  const circles = vehicles.data?.flatMap((vehicle) => vehicle.circles.map((circle) => ({ ...circle, vehicle_name: vehicle.name }))) ?? []
  const selected = circles.find((circle) => `${circle.platform_code}:${circle.external_id}` === circleKey) ?? circles[0]
  const fields = useQuery({ queryKey: ['template-fields', selected?.platform_code, selected?.external_id], queryFn: () => api<TemplateField[]>(`/template-fields${queryString({ platform_code: selected?.platform_code, circle_id: selected?.external_id })}`), enabled: Boolean(selected) })
  const upload = useMutation({ mutationFn: () => { const data = new FormData(); data.set('name', name); data.set('file', file as Blob); return api('/templates', { method: 'POST', body: data }) }, onSuccess: () => { setName(''); setFile(undefined); client.invalidateQueries({ queryKey: ['templates'] }); toast.success('导出模板已上传') }, onError: (error) => toast.error('上传失败', { description: errorMessage(error) }) })
  async function copy(text: string) { try { await navigator.clipboard.writeText(text); toast.success('字段标签已复制') } catch { setManualCopy(text) } }
  return <div className='space-y-5'><div className='grid gap-5 xl:grid-cols-[380px_1fr]'><Card><CardHeader><CardTitle>上传 Excel 模板</CardTitle><CardDescription>模板字段标签由后端校验并生成新版本。</CardDescription></CardHeader><CardContent className='space-y-4'><div><Label>模板名称</Label><Input className='mt-2' value={name} onChange={(event) => setName(event.target.value)} placeholder='例如：标准帖子清单' /></div><div><Label>Excel 文件</Label><Input className='mt-2' type='file' accept='.xlsx' onChange={(event) => setFile(event.target.files?.[0])} /></div><Button className='w-full' disabled={!name.trim() || !file || upload.isPending} onClick={() => upload.mutate()}>{upload.isPending ? <Loader2 className='size-4 animate-spin' /> : <Upload className='size-4' />}上传模板</Button></CardContent></Card><Card><CardHeader><CardTitle>可用模板</CardTitle><CardDescription>删除会隐藏模板，历史版本和既有导出继续保留。</CardDescription></CardHeader><CardContent className='space-y-2'>{query.data?.length ? query.data.map((item) => <div key={item.id} className='flex items-center justify-between rounded-xl border p-4'><div><div className='font-medium'>{item.name}</div><div className='mt-1 text-xs text-muted-foreground'>版本 {item.versions[0]?.version ?? '—'} · {formatDate(item.versions[0]?.created_at)}</div></div><Button variant='ghost' size='icon' onClick={async () => { try { await api(`/templates/${item.id}`, { method: 'DELETE' }); client.invalidateQueries({ queryKey: ['templates'] }); toast.success('模板已隐藏') } catch (error) { toast.error('操作失败', { description: errorMessage(error) }) } }} aria-label='隐藏模板'><Trash2 className='size-4' /></Button></div>) : <div className='rounded-xl border border-dashed p-10 text-center text-sm text-muted-foreground'>暂无导出模板。</div>}</CardContent></Card></div><Card><CardHeader><div className='flex flex-wrap items-start justify-between gap-3'><div><CardTitle>可用字段标签</CardTitle><CardDescription className='mt-1'>选择已保存圈子生成完整标签；标签单元格就是第一条数据位置。</CardDescription></div><Button variant='outline' disabled={!fields.data?.length} onClick={() => copy((fields.data ?? []).map((item) => item.tag).join('\n'))}><Copy className='size-4' />复制全部标签</Button></div></CardHeader><CardContent className='space-y-4'><Select value={selected ? `${selected.platform_code}:${selected.external_id}` : ''} onValueChange={setCircleKey} disabled={!circles.length}><SelectTrigger className='max-w-xl'><SelectValue placeholder='请先保存至少一个圈子' /></SelectTrigger><SelectContent>{circles.map((circle) => <SelectItem key={`${circle.platform_code}:${circle.external_id}`} value={`${circle.platform_code}:${circle.external_id}`}>{circle.vehicle_name} · {circle.name || circle.external_id}</SelectItem>)}</SelectContent></Select><div className='overflow-x-auto rounded-xl border'><Table className='min-w-[760px]'><TableHeader><TableRow><TableHead>完整标签</TableHead><TableHead>类型</TableHead><TableHead>说明</TableHead><TableHead className='text-right'>操作</TableHead></TableRow></TableHeader><TableBody>{fields.data?.length ? fields.data.map((field) => <TableRow key={field.tag}><TableCell><code className='text-xs'>{field.tag}</code></TableCell><TableCell>{{ text: '文本', number: '数值', datetime: '日期时间', collection: '集合文本' }[field.type] ?? field.type}</TableCell><TableCell>{field.description}</TableCell><TableCell className='text-right'><Button variant='ghost' size='icon' onClick={() => copy(field.tag)} aria-label='复制字段标签'><Copy className='size-4' /></Button></TableCell></TableRow>) : <TableRow><TableCell colSpan={4} className='h-32 text-center text-muted-foreground'>{circles.length ? '正在加载字段标签…' : '保存圈子后即可生成完整字段标签。'}</TableCell></TableRow>}</TableBody></Table></div></CardContent></Card><Dialog open={manualCopy !== undefined} onOpenChange={(open) => !open && setManualCopy(undefined)}><DialogContent><DialogHeader><DialogTitle>手动复制字段标签</DialogTitle><DialogDescription>当前浏览器上下文未开放剪贴板写入，文本已全选。</DialogDescription></DialogHeader><Textarea readOnly rows={12} value={manualCopy ?? ''} onFocus={(event) => event.currentTarget.select()} autoFocus /></DialogContent></Dialog></div>
}

type TemplateField = { tag: string; field: string; type: string; description: string }
