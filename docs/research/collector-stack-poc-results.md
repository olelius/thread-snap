# 采集框架 PoC 结果

## 1. 当前结论

- 当前状态：目标 Linux 首轮 2000 条已完成复核，Candidate A 与 Candidate B 均未通过本轮硬门禁。
- 该结论只绑定当前候选版本、测试包、固定输入、账号会话、网络出口、Linux 主机和测试时间；尚未形成正式技术栈选型。
- 两个候选框架保持不变。开发电脑上的纯匿名直接 HTTP 阶段 2A 已完成同一固定500条、并发1对比：A/B 均为0/500有效，因此该访问模式当前失败，不进入并发2/4/8或目标 Linux 打包。
- Candidate A 另已完成帖子字段与一级评论的HTTP API扩展验证；该结果证明当前API路径和速度可行，但500条严格完整率为74.4%，不改变两个候选尚未通过正式硬门禁的总判断。

## 2. 轮次身份与证据

| 项目 | Candidate A | Candidate B |
|---|---|---|
| 固定技术 | Scrapling 0.4.12 | Crawlee 3.18.0 + Playwright 1.62.1 |
| 结果目录 | `artifacts/poc/results/candidate-a/round-1-20260810T204251+0800/` | `artifacts/poc/results/candidate-b/round-1-20260810T210023+0800/` |
| `SHA256SUMS` 文件 SHA-256 | `4ca6c73387d36fc2f8628c188c1c1643882d5b5caed5da07d31687e34a1cfb8d` | `811bac33f94d7697e6b590bada706360f4316b7c9034da6c667d4d8c12c6e9d5` |
| 包内结果校验 | 8/8 一致 | 8/8 一致 |
| 输入数量/唯一数 | 2000/2000 | 2000/2000 |
| 输入清单 SHA-256 | `4558a54cbe96259c1a64d6fda02658b3b344b8a269fcd85ea32a793572ea5d70` | 同左 |
| 输入顺序 | 与 B 完全一致 | 与 A 完全一致 |
| 结果数量/唯一数 | 2000/2000 | 2000/2000 |
| 缺失/额外/重复结果 | 0/0/0 | 0/0/0 |
| 登录预检 | `post/success` | `post/success` |

原始 URL、逐 URL 结果、请求事件和资源采样只保存在被 Git 忽略的 `artifacts/poc/`；本报告不列出完整 URL、Cookie、令牌或其他凭证内容。

## 3. 首轮统一结果

| 指标 | Candidate A | Candidate B | 硬门槛 |
|---|---:|---:|---:|
| 有效帖子证明 | 0/2000（0%） | 315/2000（15.75%） | 2000/2000（100%） |
| 帖子 ID 匹配率 | 0% | 15.75% | 100% |
| 内容证明率 | 0% | 15.75% | 100% |
| 最终 `login` | 374 | 22 | 0 |
| 最终 `empty` | 1626 | 1580 | 0 |
| 最终 `error` | 0 | 83 | 0 |
| 未恢复控制数 | 374 | 22 | 0 |
| 结果目录总时长 | 1052 秒 | 6744 秒 | 不超过 3600 秒 |
| 运行器退出码 | 0 | 143 | 0 |
| 本轮结论 | 失败 | 失败 | 每轮全部满足 |

Candidate B 的框架日志记录 `requestsFinished=2000`、`requestsFailed=0` 和 `crawlerRuntimeMillis=925738`。这些字段表示 Crawlee 队列和处理器已结束，不等于统一访问契约成功；统一校验器记录的有效帖子证明仍只有 315 条。

## 4. 分层观察

### 4.1 已确认事实

1. 两个候选使用完全相同的 2000 条输入和顺序，结果均覆盖全部输入，输入差异不是本轮结果差异的来源。
2. Candidate A 登录预检成功，但吞吐请求没有任何帖子取得 ID 与标题/正文证明。其前约 4 分钟的请求事件主要为跳转登录页，之后主要变为停留在输入地址的空文档。
3. Candidate B 在前约 7 分钟取得绝大多数有效帖子证明；之后请求事件几乎整体变为 `empty`。最终 315 条成功、83 条 `error`、22 条 `login` 和 1580 条 `empty`。
4. Candidate B 的队列处理在约 15 分 26 秒结束，但 Node 进程没有随之退出；人工 TERM 后 Shell 包装器继续生成了 `summary.json`、`environment.json` 和 `SHA256SUMS`，最终退出码被真实记录为 143。
5. 资源峰值没有达到已知主机内存上限：A/B 最大 RSS 分别约 3.59 GiB 和 3.76 GiB；但该事实不构成访问成功证明。

### 4.2 合理推测

- Candidate B 从可访问帖子突然转为大面积空文档，时间边界明显，结合登录重定向，较符合持续负载触发访问条件变化的表现。
- Candidate A 的登录预检与并发访问结果不一致，优先检查 Scrapling 同一持久会话在多页面并发下的 Cookie/上下文连续性，以及页面返回空文档前后的主文档响应链。
- Candidate B 的进程滞留属于运行器生命周期缺陷；它与平台页面是否产生有效内容是两个独立问题。

### 4.3 尚未确认

- 当前结果没有保存首个 `empty` 页面对应的脱敏标题、文档长度、已知挑战标记和主文档响应链，因此尚不能把 `empty` 精确归因为验证码、限流、挑战脚本未完成或资源路由影响中的某一种。
- 当前没有逐页面 Cookie 内容；后续只记录 Cookie 数量与名称哈希等非敏感连续性证据，不记录 Cookie 值。

## 5. 下一阶段门

1. 已在两个固定候选中完成有界脱敏诊断：每种 `login/empty` 最多记录 3 条 URL 哈希、最终路径类型、文档长度与哈希、DOM 形态、控制标记、主文档状态链及 Cookie 数量/名称集合哈希；不保存页面正文、Cookie 名称/值或完整 URL。
2. Candidate B 已在 `crawler.run()` 返回后写出完成标记并显式退出；Linux 包装器现以独立进程组执行候选，在入口退出、硬截止或信号中断时对 npm、tsx 和浏览器后代执行 TERM/KILL 有界收口。
3. 本机合成浏览器验证已分别复现 Candidate A 与 B 的 `empty`，确认诊断文件落盘且 B 正常退出；目标 Linux 仍需运行固定前 500 条的 `test-access-transition.sh`，才能根据真实 `access-diagnostics.jsonl` 判断 `empty` 的具体页面形态。该诊断不计为 2000 条正式轮次。
4. 源码提交 `2913d3bd00c75c2e32e6625c1e7eca327c192d0e` 已形成 v0.2.15 完整包和免重装热修包，SHA-256 分别为 `70ac81c451eac1a84cf1e65be7519f9407a986d5741a60c59f22d16af43a126d` 与 `6b5c4cc85dbb6dc9c780179f4042905b85237a97bb611d5faac244daade52ceb`。目标 Linux 定向诊断确认分类证据和进程退出后，再以该版本重新开始三轮 2000 条硬门禁；当前失败轮次永久保留且不覆盖。

## 6. 500 条状态转变诊断

| 项目 | Candidate A | Candidate B |
|---|---:|---:|
| 结果目录 | `artifacts/poc/results/candidate-a/access-transition-20260810T233925+0800-20260810T233925+0800/` | `artifacts/poc/results/candidate-b/access-transition-20260810T233925+0800-20260810T234359+0800/` |
| `SHA256SUMS` 文件 SHA-256 | `dc4851bc17d27f25be7a681956cf21e94f6f413e72f236b0329a8fc51c44836c` | `097a2c4d4d391bc29ed849d00809e903de37a5fd8a13159c1a9474ee966fa124` |
| 包内结果校验 | 9/9 | 9/9 |
| 输入文件 SHA-256 | `cbb34154b1614e417c24f049844288864f139683c62a419cdab5b172e878822a` | 同左 |
| 有效帖子 | 0/500 | 71/500 |
| 最终 `empty` | 405 | 405 |
| 最终 `login` | 95 | 1 |
| 最终 HTTP 404 `error` | 0 | 23 |
| 首次 `empty` | 约64.2秒 | 约69.2秒 |
| 时长/退出码 | 274秒/0 | 224秒/0 |

已确认事实：

1. Candidate A 的 `empty` 原始响应体为0字节；Candidate B 的39字节 DOM 哈希对应浏览器为空响应补出的 `<html><head></head><body></body></html>`。两边主文档均为 HTTP 200、最终仍是帖子路径，且没有验证码、滑块或挑战标记。
2. 两边405个最终 `empty` 中有393个输入相同；差异主要分布在约一分钟的切换边界。A/B 的立即重试都没有出现 `empty -> post` 恢复，排除单次瞬时读取失败作为主要解释。
3. Candidate B 在 Candidate A 已进入大面积空文档后使用同一服务器出口立即取得71个有效帖子，随后才转为空文档。因此控制不属于纯 IP 统一阻断，更符合会话、客户端身份、并发状态和账号条件共同参与的静默访问控制。
4. Candidate A 登录预检成功，但批量访问开始即大量跳转登录；Cookie 数量和名称集合未整体消失。该候选除共同空文档控制外，还存在旧会话或并发上下文未被平台持续认可的问题。
5. Candidate B 正常响应阶段确认23个输入当前为 HTTP 404；这些输入不计为平台控制，也不进入后续正式100%门禁清单，替换后必须生成新的固定清单身份。

下一步：保持 Scrapling 0.4.12 与 Crawlee 3.18.0/Playwright 1.62.1 不变，分别刷新候选会话后执行固定前500条、并发1的诊断。每次只运行一个候选，记录是否仍在约一分钟后转空，以及是否在900秒比例观察线内完成；该结果仍不计为正式2000条轮次。

执行包：源码提交 `866f28ea665400c5489e5ad4ca0310b61cd13524` 对应 v0.2.16；完整包 SHA-256 为 `f56e62a84022df1328c3a06c62d1723831bb464b385ab2611d1923ba7ff25b58`，免重装包 SHA-256 为 `201b936d87101af9fa3cdfa272e12dd1374826ba78a2294288c23ed573fde794`。

## 7. Candidate A 现有会话主动探测

- 结果目录：`artifacts/poc/results/candidate-a/single-concurrency-probe-20260811T115350+0800-20260811T115350+0800/`；包内结果 9/9 校验一致。
- 会话状态文件年龄约70193秒、大小62293字节；年龄只作信息证据。
- Scrapling 0.4.12 在并发1下对首条已知样本取得主文档 HTTP 200，但登录预检分类为 `empty/failed`，没有帖子 ID、标题或正文证明；批量URL请求数为0，运行器以登录初始化失败退出，因此500条计时尚未启动。
- 随后使用同一旧浏览器资料打开同源登录入口，主文档 HTTP 200、DOMContentLoaded 与 load 均完成且后台待定请求为0，但10秒内没有出现短信输入控件；结果为 `TimeoutError`、`sms_page_ready=false`、`submitted=false`。这排除文件年龄门禁和500条并发作为本次失败来源，确认旧候选资料当前既不能访问文章，也不能完成重新登录页面动作。
- 当前证据尚未区分约19.5小时后的自然会话过期与平台对该客户端身份的静默控制。下一因果测试保持框架不变，使用全新临时浏览器资料完成短信初始化；成功后才整体提升为候选主资料，再立即执行同一500条并发1诊断。

执行包：源码提交 `cb6f0bfc5fe4786cec34576c3ef29f8a05b8559f` 对应 v0.2.18；完整包 SHA-256 为 `d77f8917afce386acae14d016d9f84ce13fad66d9564e3a0c19c87a35f19f1c7`，免重装包 SHA-256 为 `f1ac7d89b08cef4e5111f8e3e75421c27d6f88cbb28de50af3373ad587e2de5f`。

## 8. 本机纯直接 HTTP 500 条预筛

本轮只验证帖子主文档和内嵌状态，不请求评论接口；不启动浏览器、不导入浏览器状态、不生成动态参数，也不在失败后切换通道。两个候选顺序运行，同为匿名访问、并发1、每 URL 一次请求。

| 项目 | Candidate A | Candidate B |
|---|---:|---:|
| 直接 HTTP 实现 | Scrapling `Spider + FetcherSession` | Crawlee `CheerioCrawler + SessionPool` |
| 结果目录 | `artifacts/poc/results/candidate-a/direct-http-500-20260811T234144+0800/` | `artifacts/poc/results/candidate-b/direct-http-500-20260811T234144+0800/` |
| `SHA256SUMS` 文件 SHA-256 | `788551cc3f24784f902f626590488dc6311fb77b20fb5355f0b058cad3804b8b` | `99f3dabae81966d47a766fd8296c54ee73668e754e1b0b97c48f81b7d3e892e2` |
| 包内结果校验 | 5/5 | 5/5 |
| 输入/结果/请求事件 | 500/500/500 | 500/500/500 |
| 输入文件 SHA-256 | `cbb34154b1614e417c24f049844288864f139683c62a419cdab5b172e878822a` | 同左 |
| HTTP 通道占比 | 500/500（100%） | 500/500（100%） |
| 浏览器启动 | 否 | 否 |
| 有效帖子证明 | 0/500（0%） | 0/500（0%） |
| 最终分类 | 500 `login` | 497 `login`、2 `captcha`、1 `challenge` |
| 首次控制 | 约0.312秒，`login` | 约1.004秒，`challenge` |
| 总时长 | 71.803秒 | 74.580秒 |
| 原始处理速度 | 约6.96 URL/秒 | 约6.70 URL/秒 |
| 有效链接速度 | 0 URL/秒 | 0 URL/秒 |
| 请求放大率 | 1.0 | 1.0 |
| 统一契约错误 | 0 | 0 |
| 本轮结论 | 失败 | 失败 |

已确认事实：

1. 两个候选均完整处理同一500条输入，逐 URL 结果没有缺失、额外或重复；原始请求处理速度已超过“十万条按8小时计算”所需的3.47 URL/秒，但500条全部未取得帖子 ID 与标题/正文证明，因此该速度只代表控制页处理能力。
2. Candidate A 从第一条开始全部进入登录分类；Candidate B 从第一条开始出现挑战，最终绝大多数为登录并另有2条验证码。控制不是运行约一分钟后才出现，而是匿名直接 HTTP 的首条阶段门即失败。
3. 两端的 `channel` 均只有 `http`，`environment.json` 均记录 `browser_started=false`，请求放大率均为1.0；本轮没有浏览器兜底或动态参数路径混入。
4. 统一校验器对两端各500条均得到 `contract_error_count=0`、`success_count=0`、`unrecovered_control_count=500`。HTTP 200 或队列完成均没有被写成有效帖子。
5. 本轮 A/B 的逐 URL P50/P95 起始点存在实现差异：A 包含 Spider 队列等待，B 从实际导航开始。该问题不影响总时长、最终分类和0条有效结果；代码已统一为后续轮次都从批量提交时计时，本报告不使用本轮 P50/P95 做候选比较。

结论边界：该结果证明当前开发电脑、当前网络出口、匿名且纯直接 HTTP 的帖子主文档方案不可行；它不证明带独立认证条件的纯 HTTP、评论动态参数接口或目标 CentOS 结果必然相同。依据阶段 2A 的停止规则，本轮不继续提高并发，也不把当前实现打包到目标 Linux。

若继续研究直接 HTTP，下一步应建立新的“认证条件下纯 HTTP”独立实验：先只用3条固定样本验证会话建立、身份一致性和帖子证明；不得与本轮匿名结果拼接，也不得在 URL 失败后切换浏览器。只有3条全部有效，再重新制定500条负载。

## 9. 新浏览器与匿名 HTTP 会话对照

2026-08-12 在当前开发电脑使用全新的应用内浏览器分别打开固定联通样本和项目讨论中曾验证过的另一条帖子；两条均由帖子 URL 直接进入 `/login-required?redirect=...`，可见页面为手机验证码/密码登录表单，没有帖子正文。当前没有连接可控制的 Chrome 用户会话，因此本轮浏览器不具备既有登录状态，也没有可用于 HTTP 对照的认证材料。

随后使用 Candidate A 的 `FetcherSession` 做两组纯 HTTP 请求：

1. `impersonate="chrome"`、`stealthy_headers=true`、中文 `Accept-Language` 和站点来源头，直接访问帖子；
2. 使用相同 HTTP 会话先访问站点首页，再携带该会话建立的匿名 Cookie 访问帖子。

两条样本的结果相同：直接访问和首页预热后访问均先收到文章 URL 的 HTTP 302，再到 HTTP 200 登录页；首页本身 HTTP 200，并在响应中提供5个 Cookie 名称，但这些匿名 Cookie 没有改变帖子访问结果。原始 Cookie 值、Cookie 名称、页面正文和完整查询串未保存，结果只记录路径模板、Cookie 数量及名称哈希。

已确认事实：

- 上一轮500条并非完全没有指纹处理：Candidate A 已启用 Chrome TLS/HTTP 模拟和隐蔽请求头，Candidate B 的 CheerioCrawler 使用 Crawlee HTTP 栈和单会话 Cookie 持久化；
- 两个新浏览器样本和对应 HTTP 请求都在首个文章主文档阶段进入登录，不是运行一分钟或高并发后才出现；
- 首页匿名 Cookie、Chrome 模拟请求头和同会话预热不足以取得帖子内容；当前缺少的是平台认可的认证会话或其他尚未识别的访问条件；
- 文章主文档在页面运行前已被服务器302到登录入口，因此 `msToken`、`a_bogus` 或评论分页参数不是这一步的直接阻塞点。

结论边界：本轮没有已登录浏览器，因而没有执行“已认证浏览器状态传给 HTTP”的试验；当前证据也不能单独区分账号认证、网络出口和平台策略各自的影响。下一步需先在受控浏览器中形成真实可复访的已认证帖子会话，再以同一条样本对比浏览器复访和只导入最小认证状态的 HTTP 复访；会话值仅留在被忽略的临时文件，不进入日志、结果报告或 Git。

证据目录：`artifacts/poc/results/candidate-a/browser-http-session-probe-20260812T071759+0800/` 与 `artifacts/poc/results/candidate-a/browser-http-session-probe-known-url-20260812T071923+0800/`。

## 10. Candidate A 认证纯 HTTP 首版

2026-08-12 在当前开发电脑完成独立认证实验。当前日常 Chrome 页面已确认登录可见，但该 Chrome 未开启 CDP，项目没有直接读取、复制或记录其 Cookie；本轮改由 Scrapling `AsyncDynamicSession` 使用项目既有测试账号建立项目自有认证状态。密码登录成功、未出现二次验证，单帖取得 `post/success`，随后显式导出只保存在 `artifacts/poc/` 的 Playwright storage state。

认证状态再由 `session_handoff.py` 过滤为目标域、未过期 Cookie，并交给 Scrapling `Spider + FetcherSession + Request/Response`。框架化最终复跑如下：

| 指标 | 结果 |
|---|---:|
| 固定样本 | 3 |
| 并发 | 1 |
| 有效帖子证明 | 3/3（100%） |
| 最终分类 | 3 `post` |
| HTTP 状态 | 3 `200` |
| 总时长 | 1.169秒 |
| 有效速度 | 约2.57 URL/秒 |
| 请求放大率 | 1.0 |
| 首次控制 | 无 |
| 框架状态码阻断 | 0 |
| 项目内容阻断 | 0 |
| 源/目标有效/过期/非目标域 Cookie 数 | 39 / 32 / 2 / 5 |

已确认事实：

1. 在本机当前账号、网络和测试时点，认证状态能够从 Scrapling 浏览器 Session 交接到 `FetcherSession`，三条主文档无需逐 URL 启动浏览器即可取得有效帖子内容。
2. 匿名500条全部登录与认证3条全部成功共同说明：`FetcherSession` 或Chrome模拟本身不是已确认失败点，认证条件对本轮主文档结果有决定性差异。
3. 采集链中可由框架完成的 Session、TLS/请求头、Cookie、Spider调度、Request/Response、CrawlStats和JSONL导出已使用 Scrapling；项目只保留帖子真实性、HTTP 200控制页细分、脱敏诊断、摘要与校验。
4. 本轮没有风控响应，因此没有具体风控原因可归因；探针已能在后续出现 `login/captcha/challenge/rate_limited/empty` 时给出响应层分类，但不能据此证明平台内部评分规则。

结论边界：3条、约1.17秒不构成持续吞吐证据，不外推500条、十万条/夜、评论接口动态参数、CentOS或长期会话有效性。下一步先设计同一认证HTTP架构的固定中等样本和会话寿命阶段门，通过后才递增负载/并发。

证据目录：认证初始化 `artifacts/poc/results/candidate-a/auth-bootstrap-20260812-130809/`；认证HTTP框架化复跑 `artifacts/poc/results/candidate-a/auth-http-framework-20260812-131028/`。后者5/5校验一致，`SHA256SUMS` 文件 SHA-256 为 `7fb8dda314b753af0366362dbcf590e456e36f062878aa3ad8152ba864627e20`。

## 11. Candidate A 认证纯 HTTP 500条并发1

三样本通过后，使用同一认证HTTP架构对固定清单前500个不同URL执行完整轮次。Spider配置为并发1、每URL一次请求、请求放大率1.0；若出现首个登录、空文档、验证码、挑战或限流，则调用Scrapling `pause()`停止剩余队列。本轮没有触发暂停。

| 指标 | 结果 |
|---|---:|
| 输入/结果/请求 | 500 / 500 / 500 |
| 输入清单 SHA-256 | `cbb34154b1614e417c24f049844288864f139683c62a419cdab5b172e878822a` |
| 有效帖子证明 | 399/500（79.8%） |
| HTTP状态 | 399个200、101个404 |
| 登录/空文档/验证码/挑战/限流 | 0 / 0 / 0 / 0 / 0 |
| 总时长 | 166.031秒 |
| 处理速度 | 约3.01 URL/秒 |
| 有效速度 | 约2.40 URL/秒 |
| 请求放大率 | 1.0 |
| 正确性门 | 失败 |

已确认事实：

1. 399个HTTP 200全部通过帖子ID与标题/正文存在性校验；101个失败全部是服务端HTTP 404，没有其他失败类别。
2. 每连续50条中的404数量依次为8、16、10、8、7、11、11、9、8、13；它们贯穿整轮，没有出现先成功后整体切换控制页的时间边界。
3. 101个404正文长度均为10862字节。对其中两条做有界复查，仍返回HTTP 404及相同正文SHA-256 `6b55e232c77f377133617a55d0ec7b1b77a039243fd5f7323637489cfd94ca76`；同时复查两条成功输入仍为HTTP 200有效正文。
4. 历史 Candidate B 500条浏览器转变结果中的24个HTTP 404全部与本轮101个重合。该事实支持固定输入中确有持续不可访问项，但尚不单独证明每条的删除、隐藏或路由失效原因。
5. 本轮P50/P95从批量入队时开始计时，包含Spider排队等待，不代表单请求网络延迟；总时长、完成数和有效速度不受该口径影响。

结论：当前认证Session在本轮166秒、并发1内没有观察到已识别的平台控制，匿名轮次的登录门也没有重现；但500条统一正确性门要求500/500，本轮只有399/500，因此失败。下一步先用已登录页面或来源索引有界复核101条404，替换当前不可访问输入并生成新清单哈希，再以同一配置重跑并发1。达到500/500后才测试并发2/4/8与目标Linux。

证据目录：`artifacts/poc/results/candidate-a/auth-http-500-20260812-132210/`；5/5校验一致，`SHA256SUMS` 文件 SHA-256 为 `d423c86b0c0f89b4172d07301643b30fa63b7b2bee2e9b08ce06bdf0c0ae5954`。

## 12. Candidate A 认证纯 HTTP 2000条持续诊断

本轮使用固定2000条清单、并发1、每URL一次请求和首控暂停策略。旧认证状态在运行前首条即返回HTTP 200零字节，没有进入主轮次；项目自有Scrapling浏览器重新密码登录且无二次验证，刷新后的三条复检为3/3有效，随后从第1条重新开始2000条主轮次。

| 指标 | 结果 |
|---|---:|
| 计划输入 | 2000 |
| 产生终态 | 708（35.4%） |
| 有效帖子 | 568 |
| HTTP 404 | 139 |
| 首次控制 | 第708个终态，HTTP 200零字节 `empty` |
| 首控时间 | 228.635秒 |
| 剩余未请求 | 1292 |
| 请求放大率 | 1.0 |
| 控制前有效速度 | 约2.48 URL/秒 |
| 本轮结论 | 失败并暂停 |

控制分析：

1. 第708条最终URL与输入URL哈希一致、HTTP 200、正文0字节，Scrapling默认状态码阻断为否，项目内容阻断为是。同一URL在历史A/B浏览器2000条轮次中也曾为空，故先做隔离对照而未直接断言整体控制。
2. 停控后使用相同认证材料分别复访该历史空URL和本轮刚成功过的已知有效URL，两者都成为HTTP 200零字节；这排除“只有第708条输入自身为空”。
3. 同一项目Scrapling浏览器profile复访固定帖子也为HTTP 200零字节，说明变化不只存在于FetcherSession的TLS/请求头层。
4. 同一电脑、同一网络下，用户日常Chrome重新导航到固定帖子后仍实时显示标题、正文和评论；因此纯IP全局封锁不符合现有证据。更合理的推测是控制绑定项目测试账号、会话、profile或客户端身份组合，但当前实验不能进一步区分这三项。
5. 139条HTTP 404继续作为输入可用性问题单独统计；它们不等同于第708条静默空文档控制，也不能计入有效完成。

本轮没有刷新认证状态后继续剩余1292条，因为这会把两个会话生命周期拼成一个轮次，破坏持续会话结论。下一步先以小样本因果矩阵分别控制账号/会话、profile、HTTP客户端身份和请求间隔；找到可持续条件后才从0重新开始新的2000条测试。

证据目录：`artifacts/poc/results/candidate-a/auth-http-2000-20260812-133930/`；核心结果与 `control-analysis.json` 6/6校验一致，`SHA256SUMS` 文件SHA-256为 `bab8b1f9f246e0f109b3b0fbee46d643c467287307917549a8ce0e14a806e521`。

## 13. 未完成段冷却恢复探测

2000条主轮次暂停一段时间后，保持原认证storage state、原profile、原HTTP配置和并发1，先对本轮曾成功的已知有效样本执行1次恢复阶段门。结果仍为HTTP 200、正文0字节、`empty/failed`，约0.228秒即暂停；31个目标域Cookie仍处于未过期状态，另有3个已过期，但Cookie存在没有带来内容恢复。

因此本次等待没有解除项目Session/profile/客户端身份组合的静默控制，剩余1292条未发送。代码已支持 `--offset 708 --limit 1292`保留原清单位置和来源SHA-256；只有重新认证并通过小样本后才使用该入口，且输出必须作为新Session恢复段独立报告，不能与旧Session的708个终态合并为2000条通过。

证据目录：`artifacts/poc/results/candidate-a/cooldown-session-probe-20260812-140937/`；5/5校验一致，`SHA256SUMS` 文件SHA-256为 `7528b47f999ed1f44bfe5513cdc36680170d3188aeca7e128dfe3b1ddba0a44d`。

## 14. 新Scrapling Session未完成段恢复测试

本轮不覆盖旧profile，而是由Scrapling建立新的隔离profile并重新密码登录。登录提交成功、无二次验证，浏览器样本取得真实帖子；新的storage state交给 `Spider + FetcherSession` 后，3条纯HTTP门禁为3/3有效、1.088秒、请求放大率1.0，没有登录、空文档、验证码、挑战或限流。

随后以原固定清单SHA-256 `4558a54cbe96259c1a64d6fda02658b3b344b8a269fcd85ea32a793572ea5d70`、`offset=708` 启动剩余1292条独立恢复段：

| 指标 | 结果 |
|---|---:|
| 计划输入 | 1292 |
| 产生终态 | 767（59.37%） |
| 有效帖子 | 640 |
| HTTP 404 | 126 |
| 首次控制 | 本段第767个终态，HTTP 200零字节 `empty` |
| 首控时间 | 262.041秒 |
| 剩余未请求 | 525 |
| 请求放大率 | 1.0 |
| 控制前有效速度 | 约2.44 URL/秒 |
| 本轮结论 | 新身份暂时恢复，随后再次受控并暂停 |

控制分析：

1. 首控后只复查门禁中已确认有效的1条样本，立即再次得到HTTP 200零字节；因此不是恢复段第767条输入自身异常，而是该新Session/profile/HTTP客户端身份组合已整体转空。
2. 门禁时34个目标域Cookie有效；控制后有2个显示过期。匿名核对到期时间后确认，这2个Cookie在恢复段开始后约2至5秒即到期，而前760余个请求随后仍取得帖子或标准404，首控发生在262秒且附近没有Cookie到期。因此当前客户端Cookie自然到期不能解释首控时点。
3. 旧持续轮次在第708个终态、228.635秒转空；新恢复段在第767个终态、262.041秒转空。两次都在并发1、约3请求/秒的相近规模出现，但只有两轮证据，尚不能区分累计请求数、持续时间、请求速率、账号信誉、profile或HTTP客户端身份。
4. 新Session恢复成功证明旧项目身份当时不可用且刷新身份能暂时恢复；它不证明旧Session只是自然过期。本轮同时更换了Session与profile，不能把恢复归因于单一变量。
5. 本段对应原清单第709至第1475条的终态范围，原第1476至2000条未请求；本段不得与旧Session前708条合并为2000条通过。

下一步不继续剩余525条，改为每个实验臂使用独立新Session/profile和固定有效样本，至少比较当前约3请求/秒、1请求/秒与分批暂停，以首控请求序号和首控时间区分请求量、持续时间与速率影响。

证据目录：登录 `artifacts/poc/results/candidate-a/auth-recovery-bootstrap-20260812-141852/`；门禁 `artifacts/poc/results/candidate-a/auth-recovery-preflight-20260812-141852/`（5/5，`SHA256SUMS` SHA-256 `a7eb46ffe734a3f649afb807801d097edd1b1bf68eea170aaa88e88c7fec0e13`）；恢复段 `artifacts/poc/results/candidate-a/auth-recovery-offset708-20260812-141852/`（含 `control-analysis.json`，6/6，`SHA256SUMS` SHA-256 `ee36e7517e2e6ee8c68d4aedf4bdb4fce9f9250ca4d86df90a40727b3ba47e72`）；停控复查 `artifacts/poc/results/candidate-a/auth-recovery-postcontrol-20260812-141852/`（5/5，`SHA256SUMS` SHA-256 `8aece299b2d06ac49b5bd9fbc67601c71883e2b9310993b3835d0bb4d3e3cc40`）。

## 15. 有界Session自动恢复控制

Candidate A 已新增有界恢复状态机：

1. 现有storage state先执行3条纯HTTP门禁；
2. 批量段继续由Scrapling `Spider + FetcherSession`执行，首个 `empty/login` 立即暂停；
3. 使用 `AsyncDynamicSession` 在全新隔离profile中重新密码登录；
4. 新storage state必须通过3条固定门禁；
5. 从触发控制的URL本身重新处理，成功后再继续剩余队列；
6. 默认最多恢复2次，验证码、挑战、限流、登录或门禁失败、预算耗尽和窗口耗尽均停止。

所有门禁和重试都进入 `request-events.jsonl`及采集HTTP请求放大率；浏览器重新登录子请求不混入该指标，Session刷新次数单列。最终 `url-results.jsonl` 每个输入只保留一个最终终态。这样可以在同一轮内证明“控制发生后是否真正恢复”，而不是把不同运行目录人工拼接。

真实1条闭环使用仅含无效夹具Cookie的状态：旧门禁首条返回 `login`，控制器自动完成1次Scrapling密码登录且无二次验证；新Session门禁3/3，随后目标URL重试1/1 `post/success`。总时长16.999秒，共5个HTTP请求（旧门禁1、新门禁3、目标1），请求放大率5.0，最终完成率100%。该结果只验证恢复控制链有效，不证明2000条吞吐或每次控制都能刷新成功。

同一时段还重新门禁了两份此前返回空文档的历史状态，两者均恢复为3/3有效。它修正了“旧Session一旦转空就永久失效”的过强推断：当前只确认较短冷却探测时未恢复、约数小时后已恢复，尚未测得最短恢复时间，也未证明自然恢复与重新登录恢复属于同一机制。

证据目录：`artifacts/poc/results/candidate-a/bounded-recovery-login-smoke-20260812-171252/`；核心公开结果6/6校验一致，`SHA256SUMS` 文件SHA-256为 `e8ea5ee09bc2eae96e474040829d163daf6fbc5be35f5dd3c51335bb24f32992`。

## 16. 有界Session恢复的2000条完整验证

本轮使用原固定清单SHA-256 `4558a54cbe96259c1a64d6fda02658b3b344b8a269fcd85ea32a793572ea5d70`、全新Scrapling隔离profile、并发1、一小时窗口和最多2次恢复。初始Session及两个恢复Session的固定3条门禁均为3/3有效，无二次验证。

| 指标 | 结果 |
|---|---:|
| 计划输入/唯一终态 | 2000 / 2000 |
| 有效帖子 | 1644 |
| HTTP 404 | 356 |
| 最终未恢复控制 | 0 |
| Session恢复 | 2/2成功 |
| 总时长 | 747.120秒 |
| 采集HTTP请求数 | 2011 |
| 请求放大率 | 1.0055 |
| 有效速度 | 约2.20 URL/秒 |
| 最终有效率 | 82.2% |
| 统一正确性门 | 失败 |

恢复过程：

1. Session 1门禁3/3；批量执行708个响应，其中568条有效、139条HTTP 404，第708条为HTTP 200零字节 `empty`。
2. 自动建立Session 2，门禁3/3；原第708条重试成功，随后继续至原第1466条再次出现 `empty`。本段共759个批量响应：633条有效、125条404、1条空文档。
3. 自动建立Session 3，门禁3/3；原第1466条重试成功并完成至第2000条。本段535个批量响应：443条有效、92条404，没有再次控制。
4. 两个恢复事件均为 `trigger_recovered=true`。最终结果保持输入原顺序、2000个URL互不重复，每个URL只有一个终态；两个空文档尝试只留在请求事件中，没有成为最终失败。
5. 2011个采集HTTP请求由2000个批量基准请求、2个控制URL重试和3个Session共9个门禁请求组成；浏览器重新登录子请求不计入该HTTP指标，Session刷新次数单列。
6. 356个失败全部为服务器HTTP 404，响应体均10862字节；它们不等于已恢复的HTTP 200空文档。由于统一门禁要求2000/2000帖子证明，本轮仍失败，不能仅以完整覆盖或速度宣告通过。

结论：当前条件下，有界Session自动恢复已经真实完成两次控制后的继续处理，最多2次恢复足以让这份清单产生2000/2000终态，且在一小时内完成。但固定输入当前只有1644条可产生帖子证明，需事前构造新的当前可访问2000条清单后重新从0执行。该Windows单轮也不替代目标CentOS连续三轮硬门禁；约2.20个有效URL/秒低于十万条/8小时约3.47个有效URL/秒。

证据目录：`artifacts/poc/results/candidate-a/bounded-recovery-2000-20260812-174101/`；公开结果6/6校验一致，`SHA256SUMS` 文件SHA-256为 `91502dacb69b0d7a0ea986660e66272c71d5b5c6969a8e45e0f17c834a338c1d`。

## 17. Candidate B 认证纯HTTP与有界恢复至2000条验证

Candidate B 已补齐与 Candidate A 同结构的认证直接HTTP路径。Crawlee `PlaywrightCrawler`只用于在全新隔离profile中密码登录并导出storage state；目标域未过期Cookie随后由 `CheerioCrawler + SessionPool + persistCookiesPerSession` 持有。批量请求保持并发1，`empty/login`首控后停止旧Crawler、重新登录、执行3条门禁并从触发URL本身重试；验证码、挑战、限流、登录失败、门禁失败、恢复预算或窗口耗尽均停止。

### 17.1 三条交接门禁与500条轮次

| 指标 | 3条门禁 | 500条轮次 |
|---|---:|---:|
| 输入/终态 | 3 / 3 | 500 / 500 |
| 有效帖子 | 3 | 452 |
| HTTP 404 | 0 | 48 |
| 最终平台控制 | 0 | 0 |
| Session恢复 | 0 | 0 |
| 总时长 | 18.102秒 | 181.711秒 |
| 采集HTTP请求 | 6 | 503 |
| 有效速度 | 约0.17 URL/秒（含登录与双重小样本） | 约2.49 URL/秒 |
| 正确性门 | 通过 | 因48条404失败 |

三条门禁证明Candidate B能把项目自有浏览器认证状态交给直接HTTP，无需为每个URL启动浏览器。500条中没有观察到登录、空文档、验证码、挑战或限流；全部失败均为标准HTTP 404，必须与平台控制分开统计。

### 17.2 Crawlee MIME适配发现

首次2000条工程轮在正常帖子阶段后收到大量 `Content-Type: text/plain` 响应。CheerioCrawler默认只接受HTML/XML/JSON，因而在统一正文分类器运行前把这些响应记录成网络错误。该结果不能用于判断平台控制形态。

实现随后使用CheerioCrawler原生 `additionalMimeTypes: ['text/plain']` 接收正文，并修正同一Node进程连续重建登录会话时的请求唯一键。修正后的轮次能把相同响应识别为HTTP 200空文档，并在首个空文档处进入预定有界恢复。这里的MIME修正属于框架接收范围适配，不改变响应正文或平台状态。

### 17.3 最多2次恢复的2000条计划轮

本轮使用固定清单SHA-256 `4558a54cbe96259c1a64d6fda02658b3b344b8a269fcd85ea32a793572ea5d70`、并发1、一小时窗口和最多2次恢复：

| 指标 | 结果 |
|---|---:|
| 计划输入 | 2000 |
| 唯一终态 | 1337（66.85%覆盖） |
| 有效帖子 | 1092 |
| HTTP 404 | 244 |
| 未恢复空文档 | 1 |
| 剩余未请求 | 663 |
| Session恢复 | 2/2成功 |
| 触发URL恢复 | 2/2成功 |
| 总时长 | 488.028秒 |
| 采集HTTP请求 | 1348 |
| 有效速度 | 约2.24 URL/秒 |
| 停止原因 | `max_recoveries_exhausted` |
| 正确性门 | 失败 |

恢复过程：

1. Session 1完成625个最终结果（549条有效、76条404），原第626条成为HTTP 200空文档；Session 2重新登录与门禁成功，重试原第626条成功。
2. Session 2完成674个最终结果（519条有效、155条404），原第1300条再次成为空文档；Session 3重新登录与门禁成功，重试原第1300条成功。
3. Session 3只再形成38个终态（24条有效、13条404、1条空文档），原第1337条再次转空。此时两次恢复预算已经用完，保留该控制结果并停止，原第1338至2000条没有发送。
4. 两次恢复事件均为 `trigger_recovered=true`，说明状态机确实从触发URL继续，而非跳过控制项；但不同新Session的可用请求量差异明显，第三个Session仅维持约38个终态，不能假设“换Session固定获得约600条容量”。

随后按用户要求追加最多5次的容量观察轮，但在紧接前轮时初始Playwright页面即为 `empty`，未出现登录表单且未提交，0条进入HTTP采集。它证明重复刷新本身也会受当前访问状态约束；提高恢复上限不能替代可重复的会话建立条件，也不能用无限登录循环改写前一轮预注册停止结果。

结论：Candidate B的认证直接HTTP、SessionPool持久Cookie和两次自动恢复都已真实工作，但同口径2000条计划轮只形成1337个终态，覆盖率66.85%，没有完成2000条硬门禁。其有效速度约2.24 URL/秒高于2000条/小时最低速度，却低于十万条/8小时约3.47 URL/秒；更重要的是最终覆盖和有效率未通过，因此速度不能单独构成可行结论。当前证据下Candidate A优于B：A在同样2次恢复内得到2000/2000终态，B在第3次空文档时停止；但A仍因356条404未通过100%有效门，尚不能正式选型。

证据目录：门禁 `artifacts/poc/results/candidate-b/auth-http-gate-3-20260812-194725/`（`SHA256SUMS` SHA-256 `1531f30f0e9860c20e9ab289ec4545194da050c5760e00c3bbed55e3d7b0ce8a`）；500条 `artifacts/poc/results/candidate-b/auth-http-500-20260812-194757/`（`55d2904e65898621c9484c6500421b733d27c5f70efe76703bee6d5ffed52d99`）；最终2000计划轮 `artifacts/poc/results/candidate-b/bounded-recovery-2000-final-20260812-200550/`（`8f4e2412e6b92563051697bf08b58c56cb9dc5dab83ba012a31ab1a5e5714113`）；扩展容量轮 `artifacts/poc/results/candidate-b/bounded-recovery-2000-max5-20260812-201419/`（`d00e11b16d5c6a10098a6db4a02dd2c62dbf814ed1bd4683f6a72740917338cb`）。

## 18. Candidate A 帖子内容与一级评论 HTTP API 验证

本轮不重复实现登录：使用项目现有Scrapling登录流程生成的storage state，内容入口只注册 `Spider + FetcherSession`。帖子公开URL仅用于提取 `group_id`；程序不请求帖子DOM，每帖先请求一次详情API，评论数为0即完成，大于0才请求最多10条一级评论。可见状态、正文、媒体URL和计数全部复用详情结果。

缓存中的真实请求与快速对照确认当前路径为 `/motor/pc/ugc/detail/common` 和 `/motor/pc/ugc/detail/comment_list`。同一响应的无动态参数对照在省略 `msToken/a_bogus` 后仍取得完整JSON，详情约301ms、评论约279ms，因此测试实现未增加浏览器运行时签名。该结论仅描述2026-08-12当前接口行为。

### 18.1 单帖与小样本

- 单帖：2个API请求、649ms；正文578字、3个图片URL、1条一级评论，项目字段全部取得。
- 20条并发4：20/20严格完整，2.173秒、9.20条完整URL/秒；5条真实零评论只发1请求，其余为详情加评论，请求放大率1.75。
- 最终代码100条回归（原清单偏移620）：86/100严格完整，5.377秒、15.99条完整URL/秒；12条空详情、2条纯图片且正文文本为空。响应改为从原始字节解析JSON后，未再观察到中文字段编码失真。

### 18.2 最终500条

使用原固定清单偏移720、长度500、并发8；输入与之前内容测试区间不重叠。

| 指标 | 结果 |
|---|---:|
| 输入/唯一终态 | 500 / 500 |
| 严格完整 | 372（74.4%） |
| 总时长 | 19.381秒 |
| 处理速度 | 25.80 URL/秒 |
| 严格完整速度 | 19.19 URL/秒 |
| HTTP请求 | 703 |
| 请求放大率 | 1.406 |
| 单请求完成 | 297 |
| 页面文档请求 | 0 |
| 浏览器启动 | 否 |
| 详情/评论HTTP状态 | 全部200 |
| 图片URL | 995 |
| 视频URL | 0（本批样本未返回） |
| 一级评论 | 621 |

128个不完整终态的分类：

1. 122条API顶层 `status=0` 但详情 `data` 为空，程序以1请求结束并标记 `detail_not_found`，不再浪费评论请求；
2. 3条可见帖子有标题和图片，但正文文本真实为空；按照产品设计的“可见帖子必须取得正文”继续判不完整；
3. 1条评论接口报告总数2，但仅返回1条且 `has_more=false`，无法证明已取得全部可访问评论，保持不完整；
4. 2条详情内容完整但 `operation_status=2`；该值语义尚无样本确认，标准状态保持 `unknown`，不猜为可见或隐藏。

83条详情 `comment_count` 与评论API当前 `total_count` 不一致；两套计数均保留，一级评论完整性按评论API的 `total_count/cursor/has_more` 独立判断，不用详情计数强制额外重复请求。可选评论字段中22条作者为空、5条文本为空，评论ID、时间和点赞均已取得；这些按平台真实空值记录，不改写内容。

结论：直接JSON API路线的速度有充分余量，且无需逐URL打开页面或当前动态签名；字段完整性尚未达到第一版100%门槛。下一步不是提高并发，而是事前固定当前可用的功能样本，确认空详情来源、纯图片正文口径、`operation_status=2` 和评论计数差异，再在目标CentOS复跑相同字段契约。

证据目录：单帖 `artifacts/poc/results/candidate-a/content-functional-20260812-02/`；100条最终代码回归 `artifacts/poc/results/candidate-a/content-regression-100-offset620-c8-20260812/`；最终500条 `artifacts/poc/results/candidate-a/content-final-500-offset720-c8-20260812/`。最终500条 `SHA256SUMS` 文件SHA-256为 `5e09604a3ba3831f09bfc4f16afcd88d8ffc96d5a12559c78ff3e10f605b869f`。
