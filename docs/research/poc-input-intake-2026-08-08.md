# PoC 输入接收摘要（2026-08-08）

## 1. 输入来源

- 工作簿：`ICAR v27 垂媒监测日报-0804.xlsx`
- 使用工作表：`懂车帝`
- 使用范围：`A1:L2699`
- 原始工作簿 SHA-256：`c710f79130acd841afea0c42ebb76dae5f5375df4faa59f573b4da0d0efbccdb`
- Linux 环境截图 SHA-256：`e636e1a007f4d5a1501af32e31c02df621ab9c01a774d760773c9f45a806041c`

原始文件只保存在本地 `artifacts/poc/inputs/`，不提交 Git。

## 2. URL 整理结果

| 项目 | 数量 |
|---|---:|
| 工作表数据行 | 2698 |
| 非空原始 URL | 2698 |
| 规范化后的不同帖子 URL | 2694 |
| 排除的账号主页 | 2 |
| 合并的重复帖子记录 | 2 |

处理规则：

- 只读取“懂车帝”工作表，不混入“负面及客诉汇总”和“汽车之家”；
- 只接受懂车帝 `/ugc/article/<id>` 或 `/article/<id>` 帖子路径；
- `http` 统一升级为 `https`；
- 移除 `zt=pc_redirect` 等不影响帖子身份的查询参数和 URL 片段；
- 按规范化帖子 URL 去重；
- 排除 `/user/<id>` 账号主页。

生成的 `throughput-urls.txt` 包含 2694 行、2694 个不同 URL，SHA-256 为：

```text
82d7f4bdb766ba8d7246b04ab18e7a0a358c92616398f3eab576ef711f3f2701
```

该数量满足输入池“至少 2000 个不同 URL”的格式门槛。甲方已确认这些 URL 是真实项目会使用的测试输入；它们不是测试结果，也不代表吞吐门禁已经通过。

## 3. 来源评论数分布

| 来源评论数档位 | 数量 |
|---|---:|
| 0 | 1573 |
| 1–9 | 977 |
| 至少 10 | 144 |

这些数值来自工作簿快照，只用于挑选功能样本候选。评论数可能变化，正式功能样本仍需人工确认并填写确认时间。

## 4. Linux 环境

已从截图确认：

- 发行版：CentOS Stream 10（Coughlan）；
- 架构：x86_64；
- libc：glibc 2.39；
- 截图显示使用 root shell。

仍未确认：CPU、内存、磁盘、语言运行时、浏览器系统依赖和进程管理机制。

## 5. 本地产物

以下文件均位于被 Git 忽略的 `artifacts/poc/inputs/`：

- `throughput-urls.txt`：PoC 吞吐输入池；
- `throughput-source-index.csv`：规范化 URL 与原工作表行的本地追溯索引；
- `intake-report.json`：数量、排除项和哈希摘要；
- `linux-environment.json`：已确认环境字段与未确认项；
- `input-manifest.json`：本地输入文件大小和 SHA-256；
- 原始工作簿与 Linux 环境截图副本。

## 6. 当前缺口与下一步

1. 从 2694 条输入池固定每轮实际使用的 2000 条 URL、随机种子和清单哈希；
2. 进入候选 A/B 的访问冒烟与中等批量预筛，由程序逐 URL 验证帖子标识、内容证明和风控响应；
3. 补充 Linux 主机规格和运行时依赖，为最终 Linux 打包和三轮硬门禁做准备；
4. `functional-samples.csv` 不是当前采集框架 PoC 的必需输入，只在后续第一版功能回归阶段按需生成。
