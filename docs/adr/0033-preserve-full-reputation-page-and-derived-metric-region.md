# ADR 0033：口碑巡检同时保留完整原页与同源指标区域证据

- 状态：已接受
- 日期：2026-08-24

口碑巡检需要把紧凑截图嵌入表格行，但仅保存指标裁片会丢失车型身份和平台页面上下文，仅使用完整原页又不适合快速查看和表格交付。每个需要证据的车型平台执行项只访问并稳定捕获一次页面，保存不可变完整原页作为事实来源，同时按已验证 DOM 边界派生包含车型名称、口碑分、排名、口碑量和必要上下文的指标区域图；派生图记录原页 ID、裁剪矩形和校验值，前端默认显示指标区域并允许切换原页，两种文件共同属于一个逻辑证据，不重新访问平台，也不把裁片冒充原始页面。
