---
status: accepted
---

# 新增独立的圈内内容数，保留原口碑指标

用户确认将懂车帝车友圈“动态／最新发布”页的“共N条内容”作为独立指标加入本地巡检展示。使用新键 `circle_content_count`，不复活ADR 0039清除的旧 `circle_content`，不替代口碑量或口碑评价篇数，也不回填历史。这扩展ADR 0039“不再请求圈子页”和ADR 0064统一五指标列组的范围；原评价篇数统计对象及历史边界继续有效。

- 复用已验证的车型映射平台ID、圈子URL规则及HTTP会话，访问 `/community/{id}/dongtai-release`；核对页面圈子ID与 `cheyouList.total_count`、可见计数，单次读取不连带抓首帖详情或分页。不新增用户填写的圈子ID、URL或采集开关。
- 目前只为懂车帝声明该指标。有效0正常保存，字段未提供不补零；访问、身份、结构错误按现有异常流程处理。其余平台没有已验证来源时不增加虚假占位列。
- 正式巡检、映射验证、真实验收、比较、网页、汇报与新XLSX复用同一冻结值和来源URL；圈内内容量增减只表达数量变化，采用中性呈现，不等同于口碑改善或恶化。首个采集批次无基线，旧批次显示历史未采集。
- 圈内计数来自额外HTTP页面而非评分截图；保留其原文、规范值、来源和本次采集证明，不把评分PNG说成该计数的图像证据，不增加必需PNG数量或改写已冻结的图像与派生产物。
- 本次交付限本地更新和预览，远端应用、业务配置、已发布车型范围和调度保持原样。
