# ThreadSnap 技术路线

## 文档状态

状态：待单独评审。

本文件用于后续记录已经确认的技术路线，包括但不限于：

- 前端框架及工程组织；
- 后端语言、框架及接口规范；
- 数据库与数据迁移方式；
- 定时任务和任务并发控制；
- 平台采集器代码契约；
- HTTP 请求、HTML 解析或浏览器自动化的适用边界；
- 配置、凭证与日志安全；
- 测试策略；
- 本地运行、构建和部署方式。

当前阶段不预设具体框架、依赖版本或部署拓扑。技术选型应在首个平台接口可行性验证和运行环境确认后形成独立决策，并与 `product-design.md` 的功能边界保持一致。
