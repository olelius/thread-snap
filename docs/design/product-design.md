# ThreadSnap 功能设计

## 1. 文档状态

- 状态：初版功能基线
- 目的：规定产品功能、业务边界、分阶段交付和验收口径
- 不包含：具体编程语言、前后端框架、数据库、调度组件和部署方案

具体技术路线将在 `technical-route.md` 中单独评审和确认。

## 2. 产品定位

ThreadSnap 用于按需或按固定周期提取论坛、车友圈等社区版块中的帖子链接，并按执行批次保存结果。

每次提取都是一份独立快照。系统不持续跟踪单个帖子的后续变化，不覆盖旧批次，也不把不同批次自动合并为一条长期记录。

## 3. 交付阶段

### 3.1 第一版：内部验证版

第一版只在受控内部环境中使用，以懂车帝为首个平台形成完整业务闭环：

- 配置每周计划节点、可复用自动提取规则、平台运行参数、车型与平台圈子；
- 第一版只执行懂车帝采集，但前端展示后续平台并明确标记“暂未接入”；
- 导入已知帖子 URL 清单并手动发起提取；
- 从自动圈子、手动圈子历史或新输入圈子中选择多个圈子，配置本次统一数量后手动提取；
- 按每周计划节点选择的自动提取规则，对符合条件的平台圈子自动提取；
- 查看提取批次及状态；
- 逐条访问帖子链接并提取帖子正文及评论等相关内容；
- 查看、搜索、复制和导出链接及提取内容；
- 记录基础错误信息；
- 为后续平台预留统一采集器接口。

懂车帝的具体列表与详情接口、访问条件、分页规则、评论加载规则和状态字段仍须通过真实样本验证，不得因平台名称已经确定而假定接口可用。

第一版同时保留两种输入方式：“圈子发现提取”负责从圈子列表分页发现帖子 URL；“URL 清单提取”负责直接处理用户导入的已知帖子 URL。两种方式必须复用同一平台详情采集、标准化、存储和批次流程。

第一版使用自建 Web 前端和一个独立提取后端形成完整调用闭环。该后端同时提供面向第一版前端的页面 API 和面向后续客户现有后端的稳定集成 API，两套接口共用同一应用层、采集器、调度、Session、批次状态和提取数据库，不复制业务实现。这样后续接入客户现有模块时，只需停用第一版前端入口并让客户现有后端调用集成 API，提取后端本身不迁移、不重写。

### 3.2 后续正式版

在不重复建设公共功能的前提下，再接入两个平台。第一版前端可以选择和保存其他平台的配置与圈子链接，但后端必须显示“暂未接入”并禁止启用、手动提取或创建空任务。后续平台完成各自 PoC 后，原则上只新增平台采集器、字段映射、平台安全上限及相应测试。

如果后续平台必须依赖验证码处理、复杂动态签名、代理池或浏览器自动化，应重新评估范围和成本，不能默认视为普通适配。

若后续明确选择公网部署，必须增加身份认证和角色权限控制。具体管理框架暂不指定；业务接口和领域逻辑不得绑定到某个前端管理框架，以便后续替换管理界面或权限实现。

正式版集成到客户现有前后端系统中作为一个功能点：

- 正式前端在现有系统中新增业务界面；
- 正式前端只调用现有后端；
- 现有后端负责用户、角色、权限和公网访问；
- 现有后端通过仅限本机访问的内部接口调用提取功能服务；
- 提取功能服务持续拥有每周计划节点、自动提取规则、平台采集配置、车型圈子、提取批次和提取链接。
- 第一版前端使用的页面 API 可以退出正式部署；客户现有后端复用同一提取后端的版本化集成接口。

## 4. 用户与访问边界

- 第一版只面向单一内部使用者或受控网络环境，不部署到公网。
- 第一版不建设应用内登录、用户、角色和权限管理。
- 第一版自建基础 Web 前端和独立提取后端，用于验证完整前后端闭环；前端只调用该后端的页面 API，不调用后续集成 API，也不接触平台私有接口和认证数据。
- 正式版部署到公网前，必须完成身份认证、角色权限控制和必要的外围访问保护。

第一版的平台人工认证入口同样只在上述受控内网中使用：客户使用现有浏览器，在第一版前端的页面或弹窗中直接操作提取后端控制的服务器浏览器，不安装远程桌面客户端，不接触 SSH、CDP、VNC 地址或第二层远程桌面登录。入口通过页面 API 使用短期、一次性随机票据绑定当前认证任务，并在完成、结束、断开或超时后失效。该票据只缩小内网误用窗口，不替代公网身份认证。

公网访问属于后续可选部署增强，不是第一版交付或验收前提。只有客户以后明确需要公网部署时，才把正式前端接入客户现有身份系统，由现有后端承担用户、角色、权限、敏感操作重新认证以及 HTTPS/WSS、WAF 或等价外围保护；不得因此提前扩展第一版功能。

## 5. 核心业务对象

### 5.1 配置与采集来源

第一版将自动提取配置拆成每周计划节点、自动提取规则、平台采集配置、车型和平台圈子五个职责层。时间节点只引用规则，不复制数量等规则参数；平台页不再保存自动提取数量；车型与圈子页不保存时间、规则或平台运行参数。

#### 5.1.1 每周计划节点与自动提取规则

- 用户先维护带名称的可复用自动提取规则，再为每条每周计划节点选择一个或多个星期、24 小时制 `HH:mm:ss` 时刻、启用状态和一条或多条规则；
- 自动提取规则的可编辑字段为规则名称、明确选择的平台圈子，以及各已选平台统一的每圈有效结果目标数；规则 ID、版本和更新时间由系统生成并只读展示。同一规则可以被多个计划节点引用，计划节点不复制规则字段；
- 规则按平台折叠分组多选具体圈子；平台复选框只用于批量选择，保存时展开为明确圈子 ID。规则区只列出已经全局启用的圈子，圈子以紧凑的满宽网格行展示，不重复展示车型标题、验证状态或全局启用状态；未全局启用圈子只在“车型与圈子”管理，不进入规则选择列表。新平台或新圈子默认不加入既有规则，不能因为曾经执行“全选”而动态扩张旧规则范围；
- 节点按“星期 + 时分秒”规范化、去重和排序，使用 `Asia/Shanghai` 时区；没有启用节点表示关闭定时提取；
- 所有启用节点按逐日展开后的“星期 + `HH:mm:ss`”全局唯一：多选星期与其他节点发生任一日重叠即整次保存失败，并同时定位冲突节点、星期、时刻和规则名称。停用节点可以保留重叠时间，但重新启用时执行相同校验；系统不自动合并节点；
- 所有节点由一个全局调度源统一管理，不创建平台或圈子私有定时器；
- 服务停机期间错过的时间节点不补跑，只记录中文调度事件；已经持久化入队的任务在重启后继续处理。

“自动提取规则”和“每周计划”是配置管理中的两个同级编辑标签。用户先在规则标签维护并保存规则，再在计划标签选择已经保存的规则；两个标签分别保存当前职责范围，但后端继续使用同一提取配置 revision，并在单次事务中把当前标签草稿与服务器已保存的另一部分合并校验，避免节点引用尚未保存或已删除的规则。框架没有单一的每周循环计划成品控件，前端使用统一组件体系中的星期多选、24 小时时间输入、规则选择、按钮、标签和 Alert 组合为应用级计划编辑器，不另起视觉体系。

计划节点实际触发时冻结全部所选规则的版本、已选圈子 ID、各平台数量、实际可执行来源和运行配置快照，并把这些规则合并为一个定时批次；重复圈子只创建一个圈子任务，目标数采用所有包含该圈子的规则中最大值，同时在批次和圈子任务快照中保留来源规则。后续修改规则、平台状态或圈子状态不改变已经创建的批次。提取计划不复制平台启用状态或圈子全局自动提取开关，而是逐规则以“规则已选圈子 ∩ 已接入且启用平台 ∩ 已验证且全局启用圈子”形成来源后再求并集。

规则中的平台数量与所选圈子范围严格对应：只有至少选中一个圈子的平台才显示并保存该平台的每圈数量；取消该平台最后一个圈子时同时移除其数量。暂未接入平台只显示接入状态并禁止选择；后续平台正式接入后默认不进入既有规则。平台并发、启用状态、Session、重试与安全限制、评论数量、发现顺序、星期和时间仍不进入规则；规则本身不设置启用开关，是否触发只由每周计划节点的启用状态决定。

规则采用逻辑 ID 与不可变版本：编辑并保存规则时生成同一逻辑规则的新版本，引用该逻辑规则的计划节点无需逐条修改，下一次触发自动读取最新版本；已经创建、排队、运行或结束的批次继续使用并展示其冻结版本。任何启用或停用的计划节点仍引用规则时，归档和删除操作均被阻止并列出引用节点，用户必须先切换或删除这些节点。从未产生批次且没有节点引用的规则可以永久删除；已有历史批次但已解除全部节点引用的规则只能归档，归档后退出新节点选择器但历史仍可查看，并允许恢复为可选规则。

第一版使用全新数据库基线，不导入旧每日时间或平台自动数量，也不自动生成默认规则。首次使用时“自动提取规则”和“每周计划”均为空，用户先创建并保存至少一条规则，再创建引用它的计划节点；只有规则而没有启用节点时，定时提取保持关闭。

#### 5.1.2 平台采集配置

| 字段 | 说明 |
|---|---|
| 平台代码 | 稳定英文代码，例如 `dongchedi` |
| 接入状态 | `available` 或 `not_integrated`；由后端能力决定 |
| 平台内部并发 | 该平台全部圈子和自动重试共享的总并发上限 |
| 平台启用状态 | 是否允许创建该平台的新手动和定时任务 |
| Session 状态 | 当前认证状态、最近验证时间和认证操作入口 |

平台内部并发由前端配置，但必须服从后端按平台发布的最小值与最大值。前端限制输入范围；绕过前端提交的值由后端自动收敛到安全范围，并在响应中返回实际保存值。自动每圈提取数只在“自动提取规则”标签维护，手动提取数量只属于当前手动提交，两者都服从对应平台经 PoC 和容量验证确定的安全范围。

平台停用后不再加入新的定时任务，也不接受新的手动任务；已入队、运行中或等待认证的任务仍按入队快照完成。历史结果仍可查看、导出和删除；重新启用前不得创建手动补提。第一版只有懂车帝为 `available`，其他平台可展示和保存配置，但保持 `not_integrated` 且不能被前端开关强制启用。

后续平台从 `not_integrated` 变为 `available` 时默认保持停用，现有自动提取规则也不自动选择该平台或其圈子。平台启用只开放该平台创建新任务的全局资格，不改变规则范围；用户在“自动提取规则”标签为某条规则选择该平台圈子时，必须同时填写该平台的每圈数量。平台配置页不重复编辑规则范围或数量。

#### 5.1.3 车型与平台圈子

- 车型用于归组同一车型在不同平台上的平台圈子；同一车型在同一平台允许关联多个圈子；
- 平台圈子以“平台代码 + 平台圈子 ID”全局唯一，保存平台返回名称、规范化链接、验证状态和自动提取启用状态；
- 第一版懂车帝只实现动态版块，并按平台“最新回复”顺序发现帖子；其他版块暂不开放；
- 删除车型或圈子配置只影响后续选择和自动提取，不删除历史批次、结果或已有模板引用；圈子仍被任一未归档规则的当前版本选择时阻止删除，用户需先保存移除该圈子的规则新版本；
- 多条车型和圈子允许在前端表格中连续编辑后整批提交。整批先做格式、平台归属、圈子 ID 和重复校验；任一行错误时整批不保存，一次返回全部中文定位错误；全部通过后在同一事务中保存。

圈子允许先保存为 `unverified`。用户可以单条验证，也可以把全部未验证配置圈子一次提交到持久 FIFO；页面持续显示总数、完成数、成功、失败和等待认证数量。验证确认圈子身份、动态/最新回复列表、至少一个真实帖子 URL 和一条有效帖子结果。系统先使用未登录访问；只有明确识别为登录要求或等价身份异常时才进入官方认证流程，认证后自动重试原验证。网络错误、限流和平台控制按各自中文错误返回，不直接推断为需要登录。

配置圈子第一次验证成功时自动开启全局自动参与资格，并永久记录首次验证时间。用户此后可以手动关闭自动参与；重新验证、重新认证、圈子链接或适配器变化导致的再次验证都只刷新验证结果，不得把用户已经关闭的开关再次开启。手动圈子历史不适用首次验证自动参与。只有验证通过且全局自动参与开启的配置圈子才具备定时执行资格，具体规则范围仍须由用户明确勾选，不因验证成功自动扩张。

#### 5.1.4 手动圈子历史

用户手动输入的新圈子在验证通过后保存到手动圈子历史，记录平台、圈子 ID、平台返回名称、规范化链接和最近使用时间；不自动创建车型、不自动绑定车型，也不加入定时提取。历史永久保留到用户单条删除或清空，下次手动提取可以再次选择。以后加入自动提取时，用户必须明确选择已有车型或新建车型。

### 5.2 提取批次与圈子任务

每次手动触发或一条每周计划节点的实际触发点均创建一个独立提取批次。一个定时批次合并使用该节点引用的全部自动提取规则；一次手动批次只选择一个已接入平台，可以选择该平台的一个或多个圈子，并为所有选中圈子填写本次统一的每圈提取数。手动数量不修改自动提取规则，也不写入平台配置，但同样服从平台安全范围。

批次创建入队时冻结平台、圈子、每圈数量、平台内部并发和触发时间等业务配置。后续配置修改、停用或删除不改变已经入队的批次；执行时如后端安全上限降低，则自动收敛并记录中文说明。

建议字段：

| 字段 | 说明 |
|---|---|
| 批次 ID | 数据库内部 UUIDv7 唯一标识 |
| 批次编号 | `YYYYMMDD-HHmmss-NNN` 格式的用户可见编号 |
| 输入方式 | 圈子发现或 URL 清单 |
| 触发方式 | `manual` 或 `scheduled` |
| 创建、排队、开始、结束时间 | 用于展示队列和运行阶段 |
| 状态 | `queued` / `running` / `waiting_for_auth` / `success` / `partial_success` / `failed` |
| 平台、圈子、计划和完成数量 | 由圈子子任务汇总 |
| 错误详情 | 后端生成、可直接展示的中文说明 |
| 配置快照 | 本批次实际采用的业务配置 |
| 补提来源 | 手动补提时关联原批次 |

前端中文状态为“排队中、提取中、等待平台认证、成功、部分成功、失败”，触发类型为“手动触发、定时提取”。第一版不显示操作人，也不建设人员权限。

提取后端使用一个全局调度源，但按平台维护严格 FIFO 队列。平台认证只阻塞对应平台：该平台后续任务继续持久化排队，其他平台可以运行；认证通过后先续跑原任务，再处理该平台后续任务。第一版后端平台级并发固定为 1；后续多平台全部接入并完成组合容量验证后，平台级并发、各平台内部并发和全局资源预算作为后端重要配置重新确定，不由客户前端控制。

同一平台的多个圈子可以并行推进，但圈子列表和帖子请求、自动重试合计不得超过平台内部总并发。每个圈子保持平台最新回复顺序。圈子内按平台帖子 ID 或规范化 URL 去重；同一帖子出现在不同圈子时保留各圈子归属，可以复用同一批次内已取得的详情响应，但不能删除任一圈子的结果记录。

每圈提取数按最终有效帖子快照计数，不按已尝试 URL 数计数。自动重试只处理尚未成功的 URL；重试仍失败时继续向列表后方发现新帖子补足。达到配置数量、平台明确没有下一页或触发安全停止条件时结束。平台确实没有更多内容时，实际数量不足仍属于成功并显示中文停止原因；错误或安全停止导致不足时，有部分结果为 `partial_success`，没有结果为 `failed`。

自动重试属于原批次的正常流程，不创建新批次或重新入队。终态 `partial_success` 只手动补提失败圈子，终态 `failed` 手动补提原全部圈子；手动补提创建新的 `manual` 批次并进入对应平台 FIFO。前端把原批次和后续补提中每个圈子的最新成功结果关联汇总，但不改写或物理合并原始快照。删除任一补提批次后立即按剩余批次更新汇总，并恢复仍缺失圈子的“重新提取失败项”操作。

第一版普通 `queued` 和 `running` 不提供取消。`waiting_for_auth` 提供“结束本次提取”以解除平台队列阻塞；二次确认后按已有结果结束为部分成功或失败，关闭认证入口并允许后续手动补提。

批次数据不自动过期。只有终态可以在二次确认后永久删除；删除时在同一事务中删除该批次的圈子任务、帖子、主评论、检查点、错误明细、导出记录和文件，不影响其他批次、配置或手动圈子历史。删除配置不删除历史批次。

批次编号使用 `Asia/Shanghai` 时区，时间精度为秒；同一秒创建多个批次时，通过三位递增序号 `NNN` 区分。

### 5.3 提取链接

建议字段：

| 字段 | 说明 |
|---|---|
| 批次 ID | 所属提取批次 |
| 帖子 URL | 必需 |
| 标题 | 优先保存平台提供的帖子标题字段；平台存在多个标题字段时由适配器按已验证优先级选择，平台无标题但有正文文字时保存标准化正文第一句话；两者都没有时为空 |
| 作者 | 平台提供时保存 |
| 发布时间 | 平台提供时保存 |
| 正文 | 详情接口或详情页可合法获取时保存；平台返回 HTML 富文本时由适配器去除标签、脚本和媒体节点并保留可读段落顺序，媒体地址继续进入独立图片或视频 URL 字段 |
| 图片 URL | 正文中平台实际提供的图片地址列表 |
| 视频 URL | 正文中平台实际提供的视频地址列表 |
| 回复数 | 提取时的平台数值 |
| 点赞数 | 提取时的平台数值 |
| 版块 | 来源版块 |
| 平台帖子 ID | 平台提供时保存 |
| 可见状态 | visible / hidden / unknown |
| 原始状态 | 平台返回的原始值 |

平台未返回的非必需字段允许为空。不得为了填满表格而推断或伪造数据。纯图片或纯视频帖子在媒体 URL 已正确提取时，正文和标题为空属于平台真实内容形态，不记为字段不完整。

第一版只保存正文中的图片和视频 URL，不下载、转存或代理图片和视频文件。

### 5.4 提取评论

第一版需要采集目标帖子下可合法访问的主评论。主评论指直接回复帖子正文的一级评论；每个帖子按平台返回顺序最多保存十条，不采集楼中楼回复。评论属于对应提取批次和帖子快照，不在后续批次中覆盖旧数据。

建议字段：

| 字段 | 说明 |
|---|---|
| 批次 ID | 所属提取批次 |
| 平台帖子 ID | 所属帖子 |
| 平台评论 ID | 平台提供时保存 |
| 评论作者 | 平台提供时保存 |
| 评论内容 | 平台提供时保存 |
| 评论时间 | 平台提供时保存 |
| 点赞数 | 提取时的平台数值 |
第一版不要求采集楼中楼、超出十条的后续评论或平台未正常返回的折叠评论。

## 6. 功能模块

第一版界面以中文为主。导航、页面标题、按钮、表单标签、帮助说明、表头、分页、状态、空状态、加载提示、弹层、确认信息、校验错误和无障碍名称均使用自然中文，不沿用上游框架的英文示例文案。只有品牌名、文件格式、协议、接口和业内通用缩写等专业术语保留英文，例如 URL、ID、Session、API、SSE、WebSocket、Excel 和 XLSX；操作名称采用中文动词与必要术语组合，例如“导出 Excel（.xlsx）”和“更新 Session”。平台原有名称按其正式写法展示。

第一版采用中文直接实现，不设置语言切换入口。公共状态、分页、主题、空状态、确认和反馈文案集中维护，页面专属说明使用自然中文；日期、日历和时间格式使用 `zh-CN` 与 `Asia/Shanghai`。UI 验收必须扫描可见文字、占位文字、提示、无障碍名称、Toast 和弹层中的英文残留，只有确认过的专业术语和平台正式名称进入允许清单；帖子标题、正文、评论等平台原始内容不按 UI 文案处理。

界面主题初次访问跟随操作系统设置，顶部提供“浅色、深色、跟随系统”切换，用户选择保存在当前浏览器。导航、表格、表单、状态色、Sheet、Dialog、认证画面、加载状态和动态效果必须在浅色与深色主题下保持一致的信息层级、对比度和可用性。

第一版主视觉暂定为冷调科技方向：浅色使用冷白与淡蓝灰表面，深色使用深海军蓝与蓝黑表面，靛蓝作为主操作色，青色只承担动态指示、焦点和少量高光，成功、等待、失败和未知继续使用稳定语义状态色。轻量靛蓝到青色渐变仅用于品牌、当前导航、主操作或连接进度等有限位置，数据表格、表单和状态徽标保持清晰实色。该配色属于实施前视觉假设，允许在第一版真实 React 页面评审后整体调整，不改变页面结构和交互规则。

第一版不制作 Figma 设计稿，以真实 React 页面、组件状态和运行交互作为视觉验收对象。三个平台全部开发完成后再建立 Figma 设计稿；届时以实际前端代码、路由、组件、语义 Token、响应式断点、真实数据形态和可运行交互为来源，设计稿中的每个页面、状态和动作都必须能映射到现有前端能力或附带已通过技术评审的实现变更，不增加脱离前端组件、后端接口或运行约束的效果。

第一版不设置独立的主观视觉确认阶段，交付优先级依次为真实功能闭环、功能完整性、可用性和 UI 一致性。未来感通过统一主题 Token、公共组件和有限的命名变体实现：同一种组件、同一规格和同一用途必须使用相同的颜色、字体、间距、圆角、阴影、层级与动态；不同用途通过事先定义的变体表达，不在单个页面为某一个 Dialog、Sheet、按钮或表格临时创造一套样式。

页面反馈、加载、空状态、提示、确认和进度优先使用统一组件体系中的现有组件及其正式变体。只有现有组件无法表达真实业务语义时才增加新的应用级组合组件，新组件仍复用同一 Token、排版、图标、动态和无障碍规则，不在页面中直接拼装一次性的彩色提示块、弹窗或加载效果。

第一版响应式定位保持桌面优先：1280px 及以上展示完整布局；1024px 至 1279px 收敛次要信息，明确操作可切换为图标；768px 至 1023px 使用框架已有的动态导航 Sheet，筛选栏自动换行，数据表格保持列结构并允许横向滚动，关键标识列和操作列保持可达；低于 768px 保证基础查看与必要操作，但不另建手机卡片版。平台认证 Dialog 在各断点都使用接近全屏的操作区域。

### 6.1 配置管理

- 管理可复用自动提取规则以及引用规则的每周计划节点；
- 查看平台接入状态，配置已接入平台的内部并发、启用状态和 Session；
- 以前端表格批量管理车型和平台圈子，整批原子保存并展示后端中文校验错误；
- 单条或批量验证未验证圈子；配置圈子首次验证成功自动参与，此后重新验证保持用户当前开关；
- 查看、删除或清空手动圈子历史；
- 查看全局平台 Session 状态和最近验证时间；
- 启动官方页面人工认证、更新 Session 或清除 Session；
- 管理多个命名 XLSX 模板及其不可变版本。

配置管理页使用六个横向标签组织上述能力：

1. “自动提取规则”承载规则名称、范围、平台数量、版本与归档；
2. “每周计划”承载星期、时刻、启停状态和已保存规则引用；
3. “平台配置”承载平台接入状态、平台内部并发、启用状态、Session 状态和认证入口；
4. “车型与圈子”承载车型与多个平台圈子的批量表格编辑、圈子验证和自动提取开关；
5. “手动圈子历史”承载历史查看、选择、单条删除和清空；
6. “导出模板”承载模板上传、不可变版本、字段标签和删除。

配置项遵循唯一编辑归属：一个字段只在上述一个标签中提供编辑器，其他标签需要上下文时只显示只读摘要和“前往配置”链接。平台配置不编辑定时数量或时间，车型与圈子不编辑规则参数或平台并发，自动提取规则不编辑星期和时刻，每周计划不编辑规则范围、平台 Session、圈子资料或验证状态。手动提取中的数量只属于本次提交，不回写任何自动提取规则。

标签使用动态指示条和轻量内容过渡，切换时保持页面标题和主操作位置稳定。当前标签写入页面 URL 状态，刷新、前进和后退后仍回到相同标签；这些标签属于配置页内部区域，不增加新的顶层业务页面或第二层侧边导航。配置页的应用标题栏、页面说明、标签栏以及当前标签的标题和主操作保持在工作区顶部，只有当前标签的数据卡片、表格或表单内容纵向滚动。置顶区使用紧凑的标题、标签和卡片式工具栏间距，在保持分组边界与按钮换行空间的前提下降低纵向占用；当前标签工具栏依靠圆角、边框、轻阴影和必要摘要与页面背景建立层级，不使用横贯内容区的单色平条。

“自动提取规则”“每周计划”“平台配置”“车型与圈子”分别维护独立的未提交编辑状态，并由当前标签的显式“保存当前标签”按钮统一校验和提交；切换配置标签时保留各标签在本次页面会话中的编辑内容，保存当前标签不提交或清除其他标签的暂存内容。未保存状态使用对应标签的小圆点、当前区域工具栏计数和具体编辑项标识分层呈现：输入框、复选框、开关、星期按钮或规则多选器只有在自身规范化值偏离最近一次服务端基线时才显示琥珀色边框或实线外环，恢复原值后立即清除；新增实体使用紧邻序号或名称的新增圆点，删除项由区域计数汇总。页面标题与标签之间不插入占高的全局提示条；“车型与圈子”存在草稿时直接禁用批量验证，并依靠标签圆点、工具栏计数、保存按钮和字段外环表达原因，不再额外插入重复的保存提示卡。离开配置页面或刷新时，任一标签仍有未保存内容则提示确认；确认离开后放弃全部未保存配置。保存成功后以服务端返回的数据刷新当前标签、清除其未保存状态并显示短暂成功反馈。“手动圈子历史”和“导出模板”的上传、删除、清空等独立操作即时提交，不产生表单 dirty 标识，并按各自危险程度执行确认。

“自动提取规则”采用占满当前标签剩余工作区的主从编辑区：左侧为可按规则名称、ID 和圈子搜索的紧凑规则索引，只展示版本、圈子数、已保存计划引用数与未保存标识；右侧只渲染当前选中规则的名称、平台数量和圈子选择编辑器。桌面端由固定工具栏和弹性主从网格共同继承标签内容区高度，不以视口比例或固定像素强制撑高外层；只有规则索引或当前规则正文真实溢出时，左右面板各自滚动。切换规则只切换编辑上下文，不提交或丢弃其他规则草稿；规则数量增加时左侧索引独立滚动，不继续增加整个规则区高度。窄屏时规则索引和单规则编辑器顺序堆叠并按内容自然增长，不恢复多卡片并排。“每周计划”使用与其他可编辑标签一致的固定卡片式工具栏和节点卡片内容区，不把计划节点继续附着在规则编辑器下方。

提取规则与每周计划节点继续使用同一个全局 `revision` 和 `/extraction-plan` 单事务校验，不改为逐规则即时写入，也不新增第二套业务保存端点。前端以服务器最近一次成功响应作为共同基线，分别维护规则草稿和计划草稿；保存规则时提交规则草稿并合并服务器已保存节点，保存计划时提交节点草稿并合并服务器已保存规则，因此保存当前标签不提交或清除另一标签的未保存内容。计划多选器只显示已保存规则且至少保留一条选择，新建规则必须先保存后才能被计划引用；除原规则选择位置替换为可搜索多选器外，计划卡片其他布局不变。没有实际差异时保存按钮禁用，按钮显示当前标签待保存项总数。保存请求期间锁定当前标签的编辑控件；失败时保留草稿，并按后端明细切换到对应标签和定位规则或节点。遇到版本冲突时明确提供“保留当前草稿”和“放弃规则及计划草稿并加载服务器版本”两种选择，不静默覆盖任一侧。恢复归档规则属于即时服务器操作，存在任一规则或计划草稿时必须先保存或放弃，不能用恢复响应覆盖当前编辑。

“平台配置”中的 Session 区域保持为紧凑状态卡片，只展示平台、当前状态、最近验证时间和登录/更新、清除等入口，不承载服务器浏览器画面。配置卡片和提取列表中等待认证批次的“去认证”操作调用同一个认证流程组件；两处入口共享连接、画面、输入和完成状态逻辑，但入口所在卡片或表格行保持各自页面布局。

### 6.2 手动提取

1. 用户选择一个已接入且启用的平台；
2. 从自动圈子、手动圈子历史中选择一个或多个圈子，或者输入一个或多个新圈子链接；也可导入已知帖子 URL 清单；
3. 圈子发现方式为本次所有选中圈子填写一个统一每圈提取数，后端按平台范围收敛；
4. 新圈子先完成验证并写入手动圈子历史，同一次提交中的重复圈子链接自动去重；
5. 系统持久化配置快照并创建 `queued` 批次；
6. 圈子发现方式分页取得候选 URL，URL 清单方式直接校验、标准化并批内去重；
7. 调用统一详情流程保存帖子、媒体 URL 和最多十条主评论；
8. 达到有效结果数、没有下一页或触发安全停止条件后汇总状态。

一次手动提取只能选择一个平台，但可以选择该平台多个圈子。尚未接入或已停用的平台不能创建手动批次，并返回后端中文错误。

手动提取的前端入口固定在“提取列表”页右上角，以“新建提取”作为该页唯一主操作。点击后从右侧打开全高动态 Sheet，背景保留当前提取列表和浏览上下文；Sheet 内在“圈子发现”和“URL 清单”两种输入方式之间切换，不新增独立业务页面。提交期间主按钮显示进行中状态并防止重复触发；提交成功后 Sheet 退出，新批次以有界高亮过渡插入列表顶部。字段或业务校验失败时 Sheet 保持打开，后端中文错误显示在对应区域并聚焦首个错误。

Sheet 内采用单页渐进式表单，不使用多步骤向导。顶部为输入方式切换和平台选择，中部只展开当前模式需要的圈子选择、新圈子输入、统一数量或 URL 清单，并就地展示异步验证、去重和解析结果；底部显示本次任务摘要以及固定的取消、提交操作栏。模式切换、字段展开和验证结果使用保持空间连续性的过渡。

同一次打开 Sheet 期间，“圈子发现”和“URL 清单”分别保留各自的未提交输入，来回切换不清空另一模式；点击提交时只校验并提交当前选中的模式。关闭 Sheet、返回或切换页面时直接放弃全部未提交输入，不显示放弃确认，也不在页面刷新后恢复草稿。

### 6.3 定时提取

- 一个全局调度器加载全部启用的每周计划节点；每个节点引用一条或多条自动提取规则，并在一次实际触发点最多创建一条合并定时批次；
- 到点解析节点引用的全部有效规则，读取符合各自规则范围、已接入、平台已启用且圈子已启用自动提取的来源，按圈子求并集并对重复圈子采用最大目标数，同时冻结全部规则版本、参数、来源映射和运行配置快照；
- 没有可执行圈子时不创建空批次，只记录中文调度事件；
- 同一“计划节点 + 计划触发时刻”通过稳定幂等键防止服务抖动产生重复批次；
- 调度器加载配置时再次检查启用节点的星期与时刻重叠；发现持久数据异常时不选择其中一条继续执行，而是跳过冲突触发并记录可定位的中文配置错误；
- 节点触发前校验全部所选规则是否为各自已选平台保存数量；任一规则异常缺失时停止该节点整次触发并记录中文配置错误。规则已选但当前全局停用、未验证或所属平台停用的圈子不创建任务；合并后没有可执行圈子时不创建空批次，并记录中文调度事件；
- 定时、手动和手动补提都进入同一持久化平台 FIFO；不允许手动任务插队；
- 服务重启后恢复已入队任务，停机错过的历史节点不补跑；
- 与手动提取复用同一采集和结果流程；
- 第一版不包含邮件、短信或企业微信通知。

### 6.4 提取列表与批次详情

提取列表一行代表一次手动或定时批次；详情按平台和圈子展示子任务。列表至少展示批次编号、提取类型、创建/排队/开始/完成时间、状态、排队位置、平台数、圈子数、计划数、完成数、失败数和后端中文错误或等待认证原因。

提取列表采用数据表格，不使用卡片网格或行复选框。默认列按信息关系合并为六组：“批次编号、提取范围、状态、进度、时间、操作”。批次编号单元格次行显示触发类型和输入方式；提取范围显示平台及圈子名称或数量；状态单元格按需显示排队位置、等待认证或失败简因；进度单元格显示完成数、计划数、失败数和动态进度条；时间单元格以创建时间为主，并按状态补充开始时间、完成时间或运行时长；操作列只展示当前状态有效的查看、去认证、重新提取失败项或删除操作。点击普通行区域进入批次详情，行内操作独立触发且不同时进入详情。

操作按钮优先使用中文文字配合图标；当图标含义明确、框架已有一致图标或操作列空间不足时，可以使用仅图标按钮，但悬停和键盘聚焦时必须显示中文 Tooltip，并提供中文无障碍名称。业务含义特殊的“去认证”和“重新提取失败项”优先保留文字；删除等危险操作放入带文字的“更多”菜单并二次确认。次要操作较多时优先收敛到“更多”菜单，不把大量相近图标并排堆放。

列表工具栏提供批次编号搜索、状态筛选、触发类型筛选和创建时间范围，右侧提供“重置筛选”、手动刷新和“新建提取”主按钮。页面标题、主操作和筛选工具栏以紧凑间距固定在工作区顶部，筛选卡片不叠加无业务内容的外层纵向留白；批次列表固定表头和分页底栏，只有中间数据行区域纵向滚动。筛选作用于完整批次集合并在变化后回到第一页；筛选、页码和每页数量写入 URL，刷新、前进和后退时恢复。第一版只有一个实际接入平台，列表暂不增加平台筛选。

- 所有圈子成功时为 `success`；
- 部分圈子成功、部分失败时为 `partial_success`；
- 所有圈子失败时为 `failed`；
- 任一平台等待认证且批次未结束时为 `waiting_for_auth`，详情保留其他平台真实状态；
- `partial_success` 和 `failed` 提供手动“重新提取失败项”；
- 关联补提全部完成时允许显示衍生提示“重新提取完成”，但不篡改原批次状态；
- 终态支持二次确认后的永久删除；普通排队和运行中不提供取消；等待认证可以结束本次提取以解除平台阻塞；
- 第一版不提供复杂统计图表或操作人字段。

提取列表以服务端事件通知驱动刷新：后端在批次、圈子任务、排队位置或 Session 状态提交变化后，通过 SSE 向前端发送只含资源类型、资源 ID 和版本的轻量变化信号；前端收到信号后查询当前可见列表或对应详情，以 HTTP API 返回的数据作为权威状态。SSE 重连成功、页面重新获得焦点、网络恢复或完成新建、补提、结束认证等待、删除等操作后立即刷新，另保留每六十秒一次的低频兜底和手动刷新。状态、数量和排队位置变化使用短暂过渡，不使用持续闪烁的运行效果。

“重新提取失败项”打开确认 Dialog，显示原批次编号和本次失败项数量，并说明该操作会创建新的关联批次、只处理尚未成功的 URL、原批次状态与快照保持不变。确认后提交，成功时关闭 Dialog，并在列表中展示新关联批次。

平台认证操作使用接近全屏的动态 Dialog。顶部显示平台、关联任务、认证阶段、剩余时间和连接状态，中部展示服务器浏览器画面并转发鼠标与键盘输入，底部承载重新连接、关闭窗口以及任务场景下的“结束本次提取”。画面操作必须覆盖持续鼠标移动、悬停反馈、按下、释放、拖动、滚轮、键盘组合键和粘贴，并保持输入坐标与服务器浏览器视口一致；不得退化为只有点击后才更新的静态截图。认证成功并通过后端门禁后 Dialog 自动退出，原批次恢复并以有界高亮展示状态变化。

中继连接状态与平台页面状态必须分开显示；WebSocket 已连接不等于平台页面可操作。只有官方页面形成非空、可交互 DOM 后才显示“页面可操作”并开放输入与“完成并校验”，HTTP 错误、零字节响应或空 DOM 必须在 Dialog 内显示稳定错误码和中文原因，不得继续展示空白画布。门禁未通过时保留当前认证页面供用户继续操作，只有门禁通过后才更新正式 Session、自动关闭 Dialog 并恢复等待批次。

### 6.5 链接结果

- 表格展示基础字段；
- 标题可点击跳转到原始帖子；
- 支持标题关键词搜索；
- 支持单条复制和批量复制链接；结果表格不提供复选框，单条复制只写入该帖的规范化原帖 URL，批量复制写入当前标题搜索、圈子和可见状态筛选共同命中的全部结果，不受当前分页限制；
- 支持查看帖子正文和最多十条主评论；
- 支持按圈子和可见状态筛选；
- 导出只读取数据库中已经持久化且不会再变化的终态结果，不重新访问平台。

批次详情页上方以紧凑布局展示返回入口、批次标题与操作、摘要和搜索筛选工具栏，下方展示链接结果表格。圈子任务不再占用置顶区独立行；“圈子任务 N”按钮紧邻刷新操作，点击后打开按平台分组的可滚动 Dialog。Dialog 顶部汇总任务总数、成功数、结果进度和异常数，各平台分组只展示一次平台名称与聚合进度；圈子行展示名称、原帖入口、状态、结果进度和必要结果说明，不重复平台列，也不把正常终止说明误标为错误详情。批量复制与导出模板选择共同位于筛选工具栏右侧，作为相邻的结果输出操作，不在列表底部另设孤立导出行。桌面布局中上述上下文保持在工作区顶部，链接结果区同时固定表头与分页底栏，只有中间数据行区域纵向滚动；横向滚动与数据行共用同一视口，避免表头和数据列错位。窄屏内容超过可用高度时回退为整页顺序滚动，但结果区仍使用内部滚动保留表头和分页操作。帖子标题作为平台原帖链接并在新标签打开；每行另设“查看”操作，从右侧打开约占页面宽度 50% 至 60% 的大型动态 Sheet，展示本批次数据库快照中的标题、作者、时间、可见状态、正文、图片与视频 URL、回复数、点赞数、最多十条主评论、原始状态和诊断信息。两种操作必须使用不同的可见文案和图标，避免把本地快照误解为平台实时页面。

配置页中具有列标题的数据列表复用同一滚动约束：表头固定，仅数据行区域滚动；没有分页的列表不额外制造空白底栏。基础表格组件不自行创建第二层横向滚动容器，每个页面的数据视口统一承载纵向和横向滚动，保证粘性表头始终与内容使用同一个滚动参照。

所有数据表格的第一列统一显示“序号”。提取列表和批次结果等请求分页列表按当前筛选与排序后的完整结果集连续编号，翻页后从上一页末尾继续；车型与圈子、手动圈子历史、导出模板字段等不分页配置列表按当前展示顺序从 1 编号。序号仅用于界面定位，不写入数据库、接口、URL 状态或导出字段。

帖子结果表默认列为“标题、圈子、作者、发布时间、可见状态、评论数、点赞数、操作”。标题列获得主要可用宽度，操作列固定在右侧并提供“查看”和“复制链接”；第一版的平台名称放在批次摘要中，不在每一行重复展示。完整 URL、正文、媒体 URL、评论内容、原始状态和诊断信息只在快照详情 Sheet 中展开。

帖子结果表复用统一分页控件并采用请求分页：默认每页 50 条，可切换 20、50 或 100 条；页码、每页数量、标题搜索、圈子筛选、可见状态筛选和排序变化共同触发后端查询，搜索、筛选或排序变化后回到第一页。后端返回当前页结果和完整筛选结果总数，界面显示当前范围及总数；翻页加载期间保留上一页布局并显示轻量加载状态。

帖子结果表默认使用稳定的来源顺序：URL 清单保持用户输入顺序；圈子发现保持平台列表发现顺序；多个圈子按本次提交时的圈子顺序排列；补提成功结果回到原失败 URL 的逻辑位置，而不是追加到结果末尾。用户可以切换按发布时间、评论数或点赞数排序，排序作用于完整筛选结果并在变化后回到第一页；值相同时仍以稳定来源顺序裁决。批量复制沿用当前有效排序。

帖子结果页把标题搜索、圈子筛选、可见状态、排序字段与方向、页码、每页数量和当前查看的帖子 ID 写入 URL 查询状态。刷新、前进、后退和复制页面地址后重新打开时恢复相同上下文；同一路由内的搜索、筛选、排序和分页只更新查询参数与结果查询，不触发整页重新加载，也不重置当前滚动区位置。打开快照详情 Sheet 会增加帖子 ID，浏览器返回可关闭该 Sheet。URL 只保存筛选条件和资源标识，不包含帖子正文、评论、平台凭证或其他快照内容。

快照 Sheet 打开时保留背景表格的分页、筛选、搜索和滚动位置；关闭后保留关闭瞬间的背景滚动位置，不回到首次打开 Sheet 时的位置。遮罩区域的鼠标滚轮和触控板滚动不得驱动背景表格，Sheet 内容区域的滚动只作用于详情。Sheet 内提供上一条、下一条和关闭操作，顺序以当前筛选与排序结果为准；当前查看的帖子在背景结果表中以文字标签、浅色行面和左侧液态光晕共同标示。同页相邻切换时光晕按实际行位置上下过渡，目标行已经可见时背景不滚动，越出可视边界时只最小距离平滑滚动至可见；跨页加载后在目标行重新出现标示。关闭 Sheet 时，焦点交还给当前帖子行的“查看”按钮，当前行以“刚刚查看”标签、浅色行面和左侧光晕保留约 1.8 秒后淡出；该短暂定位提示不写入 URL，也不形成永久选中态。切换记录时保留当前快照直至新快照返回，由被点击的箭头显示局部等待状态，两个按钮的尺寸、位置和背景状态保持稳定，正文、媒体和评论使用轻量内容过渡，不重新访问平台。

批量复制使用当前批次与关联补提汇总后的稳定结果顺序，优先按平台帖子 ID 去重，缺失时按规范化 URL 去重；每条链接占一行，不添加编号、标题、引号、额外空白或末尾空行。零条结果时批量复制禁用，成功后显示实际复制数量。浏览器剪贴板写入受限时，打开手动复制 Dialog，在只读文本区域中展示并自动选中相同内容，供用户使用系统复制快捷键；模板字段标签复制复用相同的剪贴板回退行为。该规则作为第一版暂定口径，后续只有出现真实的任意选择或其他输出格式需求时再扩展。

### 6.6 XLSX 模板与导出

系统允许保存多个命名模板；同名模板重新上传时创建不可变新版本，不覆盖旧版本。导出时选择模板，而不是在创建批次时绑定模板；同一批次可以使用不同模板重复导出。

模板字段标签必须是稳定英文标识：

```text
platform.<platform_code>.circle.<circle_id>.<field>
```

标签不使用中文车型名称、工作表名称或数据库物理字段名。前端提供可用标签、数据类型、说明和复制入口。上传时校验全部标签；无效标签拒绝保存，并一次返回中文的工作表、单元格、字段和原因。模板引用的圈子合法但本次批次没有该圈子结果时，对应列留空。

第一版字段后缀注册表如下。前端按当前已保存圈子生成完整标签，例如 `platform.dongchedi.circle.24729.post.title`；每个标签单元格只能绑定一个注册字段。

| 字段后缀 | 类型 | 导出内容 |
|---|---|---|
| `vehicle.name` | 文本 | 批次快照中的车型名称 |
| `circle.id` | 文本 | 平台圈子 ID |
| `circle.name` | 文本 | 批次快照中的平台圈子名称 |
| `circle.url` | 文本 | 批次快照中的圈子规范化链接 |
| `post.platform_post_id` | 文本 | 平台帖子 ID |
| `post.url` | 文本 | 帖子规范化链接 |
| `post.title` | 文本 | 帖子标题或已确认的正文首句回退值 |
| `post.author` | 文本 | 帖子作者 |
| `post.published_at` | Excel 日期时间 | 帖子发布时间 |
| `post.content` | 文本 | 帖子正文 |
| `post.image_urls` | 集合文本 | 编号后的图片 URL，中文分号和单元格内换行分隔 |
| `post.video_urls` | 集合文本 | 编号后的视频 URL，中文分号和单元格内换行分隔 |
| `post.reply_count` | 数值 | 提取时回复数 |
| `post.like_count` | 数值 | 提取时点赞数 |
| `post.section` | 文本 | 来源版块；第一版懂车帝为 `dynamic` |
| `post.visibility` | 文本 | `visible` / `hidden` / `unknown` |
| `post.raw_status` | 文本 | 平台原始状态值 |
| `comments.content` | 集合文本 | 编号后的主评论作者、时间和正文 |
| `comments.content_with_likes` | 集合文本 | 编号后的主评论作者、时间、正文和可用点赞数 |

数据库可以在不改变这些标签的前提下调整物理表名或列名。后续新增字段只能增加新的稳定后缀；已有后缀含义发生不兼容变化时必须新增标签版本，不能静默改义。

字段标签所在单元格就是第一条帖子数据位置，导出后标签不保留，最终文件为用户表头和数据；后续帖子按各圈子结果顺序向下写入。一个帖子占一行。同一工作表可以包含多个圈子的列，各圈子第 N 条结果横向写入第 N 个数据行，数量较少的圈子对应列留空，这种并排不表示帖子之间存在业务关联。工作表数量、名称、表头、列顺序和样式由用户模板决定，后端不按工作表名称推断圈子。

字段标签不得位于合并单元格。后续数据单元格复制标签单元格的字体、填充、边框、对齐和数字格式，只复制对应单元格样式，不复制整行或移动其他区域。评论、图片和视频集合字段启用自动换行。预计写入范围内已有文字、公式或其他内容时停止导出并返回中文冲突错误；第一版不自动扩展公式，范围外的公式、图表、图片和说明保持不变。

一个帖子仍只占一行。图片和视频 URL 分别按平台顺序在各自单元格内编号，以中文分号和换行分隔。主评论默认集合字段包含作者、上海时区时间和评论正文；带点赞版本额外包含点赞数。格式示例：

```text
1. 作者：作者甲
   时间：2026-08-14 10:30:00
   评论：第一条评论；

2. 作者：作者乙
   时间：2026-08-14 10:35:00
   评论：第二条评论
```

点赞数为 0 时显示“0赞”，平台未返回点赞数时省略点赞行；作者或时间未返回时对应值留空。单值时间以 Excel 日期时间值写入，显示为 `yyyy-mm-dd hh:mm:ss` 并统一转换到 `Asia/Shanghai`；集合中的时间使用同格式文本，不以导出时间填补缺失发布时间。

相同结果版本和模板版本重复导出时复用已有文件；结果汇总变化或模板版本变化时生成新导出记录。导出记录保存批次或补提汇总版本、模板 ID、模板版本、生成时间、状态和文件哈希。`success`、`partial_success` 和有已持久化数据的 `failed` 允许导出；`queued`、`running` 和 `waiting_for_auth` 不允许导出。删除批次时删除其导出文件；删除模板仅从可选列表隐藏，被历史导出引用的版本继续保留。

## 7. 平台采集器功能契约

所有平台采集器都应具备相同的业务能力：

1. 验证配置和访问条件；
2. 根据圈子、版块和分页参数获取一页数据；
3. 判断是否存在下一页；
4. 将平台字段转换为统一字段；
5. 返回可诊断的错误类型；
6. 保留必要的原始状态；
7. 遵守统一的数量上限、超时、重试和停止规则。

公共业务只依赖统一结果，不直接读取平台私有字段。具体接口形式和代码契约留待技术路线文档确定。

## 8. 可见状态规则

- `visible`：已有样本和字段规则证明帖子正常展示；
- `hidden`：已有样本和字段规则证明帖子被隐藏、删除、审核或不可正常展示；
- `unknown`：字段缺失、新状态值或证据不足。

需求中出现的 `status`、`visible`、`audit_status`、`fold_type` 等名称只是候选线索，不是已确认字段。

懂车帝当前人工核对样本已经确认 `operation_status=2` 的帖子页面可正常展示，因此当前适配器将已验证的原始值 `0` 和 `2` 映射为 `visible`，同时保留原始值。该结论是样本验证规则，不等同于平台官方枚举说明；遇到其他新值仍标记为 `unknown`，直到重新取得证据。

第一版因正文和评论需求需要逐条访问详情接口或详情页。可见状态判断应优先复用该次详情访问结果，不为状态判断额外重复请求。

## 9. 页面范围

第一版只包含三个基础页面：

1. 配置管理页；
2. 提取列表页；
3. 批次链接详情页。

配置管理页使用六个同级标签承载自动提取规则、每周计划、平台配置、车型与圈子、手动圈子历史和 XLSX 模板，不因此增加独立业务页面或第二层侧边导航；规则定义“提取什么”，计划定义“何时使用哪条已保存规则”，两者按唯一编辑归属分开。

第一版前端定位为现代、数据密集、桌面优先的内部运营控制台。三个基础页面共享同一个应用外壳和可收缩的左侧导航；导航展开、收缩、当前项切换以及由此产生的内容区变化必须有连续、可感知且不阻塞操作的过渡，避免以瞬间跳变代替状态反馈。

页面以信息清晰和操作可用为目标，采用克制卡片、清晰表格、稳定状态色和一致的间距、圆角、阴影及图标体系。动态效果必须服务于导航、加载、展开收起、状态变化、提交结果和错误恢复等真实因果关系；不采用持续漂浮、粒子背景、大面积视差或遮挡任务信息的装饰性动画。所有关键操作在动画过程中保持可用，并为系统减少动态效果偏好提供等价的弱化表现。

第一版不要求品牌级视觉设计或移动端专项适配，但常用桌面宽度下必须保持信息层级、表格可读性和完整操作路径；缩窄窗口时不得因导航和固定区域导致主要操作被遮挡。

## 10. 第一版验收条件

第一版至少满足：

1. 懂车帝已经完成真实样本、动态/最新回复圈子发现和 Windows 后端闭环验证；目标 CentOS 三轮门禁暂缓，仍是最终 Linux 部署验收项；
2. 可以配置可复用自动提取规则、引用规则的每周计划节点、平台运行参数、车型和多个平台圈子，未接入平台明确显示且不能创建任务；同一配置项不在多个标签重复编辑；
3. 圈子可以先保存为未验证，测试通过后才能启用自动提取；
4. 可以从自动圈子、手动圈子历史或新输入圈子手动提取，也可以导入已知帖子 URL 清单；
5. 手动提取一次选择一个平台、多个圈子和一个统一数量且不回写自动规则；定时节点按所选规则创建一条覆盖其可执行来源的批次；
6. 定时、手动和补提任务持久化排队，不插队，重启后恢复；平台认证只阻塞对应平台；
7. 每圈数量按有效结果计，达到数量或平台明确没有更多内容时正常停止；
8. 提取列表和圈子详情显示中文类型、状态、数量和后端错误详情；
9. 可以逐条访问帖子并保存真实可获取的正文、媒体 URL 和每帖最多十条主评论；
10. 终态部分成功和失败可以手动补提，自动重试仍属于原批次，补提关联展示不改写原快照；
11. 终态可以二次确认永久删除，等待认证可以结束本次提取，普通排队和运行中不能取消；
12. 可以上传、校验和版本化多个标签驱动的 XLSX 模板，并在导出时选择模板；一个帖子一行；
13. 成功、部分成功和有数据的失败结果可以查看、搜索、复制并从数据库快照导出；
14. 所有可展示错误由后端返回稳定英文错误码和中文详情，前端不维护错误文案映射；
15. 不同批次互不覆盖，平台缺失字段和未知状态不会被伪造；
16. 已预留后续两个平台的统一适配器、平台队列和组合容量控制边界；
17. 在约定的部署环境、网络条件、合法访问条件和统一测试数据集下，一小时内有效完成 2000 个帖子 URL 的访问与结果处理；
18. 连续三轮吞吐测试均满足上述条件；任何限流、验证码、挑战页、登录失效或其他平台控制导致未能完成约定结果时，该轮验收失败。

“有效完成”必须满足：系统在测试时间窗口内实际访问该 URL，正确识别平台响应，并保存通过字段校验的帖子快照或有证据支持的平台终态。排队未执行、主动跳过、超时、验证码或验证页、无法确认的空数据、测试窗口结束后的补跑以及人工修补结果均不计入完成量。

每小时有效完成 2000 个 URL 是真实交付要求，不是仅供观察的性能目标。最终验收仍需同时报告自动成功率、正文完整率和主评论完整率，不能仅以请求发出数量证明达标。

需求中所称“必须绕过风控”按上述结果门禁理解：项目不单独验收某种对抗技术名称，只验证实际访问条件下是否按时产生 2000 个完整有效结果。通过结果只证明对应版本、样本、账号、网络和测试时间窗口内达到门槛；运行条件实质变化后需要重新验证。

吞吐测试输入由甲方从已知有效链接中提供。每轮输入 2000 个不同 URL，要求 2000 个全部有效完成，最终完成率为 100%，不允许存在最终 `failed`、`partial` 或未完成结果。

必需字段完整率要求为 100%：可访问帖子必须取得正文文字或至少一个平台实际返回的图片/视频 URL；纯媒体帖子允许正文为空。标题优先采用平台标题，平台无标题时取正文第一句话，标题和正文都没有时标题为空。一级评论以评论接口本次实际返回为准，达到十条时按平台顺序保存前十条，不足十条且接口明确 `has_more=false` 时保存实际返回结果并视为本次结束。详情回复数、评论接口总数与实际返回数量不一致时保留诊断字段，不据此虚构缺失评论；`has_more=true` 但游标缺失、评论接口失败或控制响应仍属于不完整。作者、发布时间、点赞数等平台本身未提供的可选字段允许为空，但系统不得因为解析失败而把已有字段静默保存为空。

字段完整率以测试时点能够取得帖子详情的结果为分母；详情已经不存在的输入单独计入“来源缺失”和“输入解析率”，不伪装成字段解析失败。正式每轮 2000 条验收仍要求事前固定 2000 条有效输入，运行时出现来源缺失仍会使该轮 2000/2000 有效完成门失败，不能通过事后缩小分母通过吞吐验收。

上述是第一版完工验收。根据 ADR 0011，圈子列表、主评论、数据库、队列、导出以及自建 React 前端已经完成 Windows 实现与真实页面联调；目标 CentOS 三轮 2000 条门禁暂缓，但仍是最终 Linux 部署验收的独立项，当前不记为已通过。

平台在正文中实际提供图片或视频地址时，应保存其 URL；媒体文件本身不进入下载和吞吐验收范围。

## 11. 第一版不包含

- 应用内登录和权限系统；
- 第二、第三个平台的实际采集器；
- 自定义 Cron；
- 独立代理池管理、验证码识别服务和反风控运营后台；
- 默认使用浏览器自动化；
- 数据趋势、敏感词和高频账号分析；
- 邮件、短信和企业微信通知；
- 长期追踪帖子变化；
- 历史数据回刷；
- 平台改版后的长期免费维护。

## 12. 商务范围参考

当前首个平台基础交付范围由以下工作构成：

- 数据库、分层配置、平台 FIFO 队列和批次管理；
- 手动、全局定时及手动补提任务；
- 三个基础前端页面；
- 懂车帝动态/最新回复采集器；
- 多版本标签模板 XLSX 导出和中文错误记录；
- 联调、测试和部署说明。

报价、付款节点和维护期限属于商务约定，不作为功能或技术事实写入本设计。
