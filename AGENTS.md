# ThreadSnap 项目全局约束

## 1. 文档职责

本文件只记录长期稳定、跨阶段有效的项目规则，不记录完整开发日志、临时结论、提交号、测试次数或逐项完成历史。

- 文档索引与读取顺序：`docs/README.md`
- 领域词汇：`CONTEXT.md`
- 功能设计：`docs/design/product-design.md`
- 技术路线：`docs/design/technical-route.md`
- 唯一活动任务账本：`WORKLOG.md`
- 跨任务工作线口径：`docs/chains/`
- 流程控制与验证阶梯：`docs/process/`
- 可复用项目记忆：`docs/memories/`
- 冻结的历史记录规则：`docs/project-notes/README.md`
- 已接受决策：`docs/adr/`
- PoC 执行计划：`docs/research/collector-stack-poc-plan.md`

### 1.1 强制读取顺序

- 所有项目任务：先检查当前 Git 状态，再读取本文件和 `WORKLOG.md` 的最新条目。
- 上下文压缩、新会话或任务恢复：必须先从最新账本条目恢复总目标、证据、下一步和边界，不重查其中已有可复核证据的事实。
- 回答“当前进度”或“下一步做什么”：同时读取 `docs/design/product-design.md`。
- 需求、功能或验收变更：读取 `CONTEXT.md`、`docs/design/product-design.md` 和相关已接受 ADR。
- 技术设计、实现、重构或部署：读取 `CONTEXT.md`、产品设计、技术路线和相关已接受 ADR。
- 触及跨任务工作线：先读取 `docs/chains/` 中对应链档，核对当前口径和未决项。
- PoC、采集器或性能测试：额外读取 `docs/chains/first-platform-delivery.md`、`docs/research/collector-stack-poc-plan.md` 及相关模板说明。
- 开始编码前必须完成上述任务对应的读取，不能只依据本文件、历史对话、历史记录或全局记忆。

### 1.2 当前事实与冲突处理

- 当前分支的 Git 状态和仓库文档是项目事实来源；历史对话、全局记忆和旧分支内容只作为检索线索。
- 功能范围和验收以产品设计为准；技术决策以已接受 ADR 和技术路线为准；跨任务当前口径以对应链档为准；任务完成情况以 Git 状态和最新 `WORKLOG.md` 条目共同为准。
- 技术调研只提供参考，不自动成为实现规范；模板和 ZIP 只用于输入或交付。
- 文档之间出现冲突时，先停止实现并同步修正唯一 owner 文档，不得复制一份新口径或自行选择有利版本继续开发。

## 2. 项目目标

ThreadSnap 是一个前后端模式开发的多平台论坛链接快照提取系统。系统按配置手动或定时提取指定平台、圈子和版块的帖子链接及列表字段，每次执行形成独立批次快照。

项目的核心是提取与管理链接快照，不是持续追踪帖子变化，也不承诺规避平台风控。

## 3. 交付边界

1. 第一版只交付一个平台的完整闭环。
2. 首个平台必须经过接口与样本可行性验证后再确定，不得根据字段名猜测直接实现。
3. 后续再交付另外两个平台。
4. 第一版必须预留统一平台采集器接口，后续平台通过新增适配器接入，不重复开发公共前后端、调度和存储能力。
5. 第一版只实现基础功能，非必要的统计分析、通知、多用户权限、代理池、验证码处理和复杂浏览器自动化不进入默认范围。
6. 平台不存在或无法可靠获取的字段允许为空；可见状态无法确认时必须标记为 `unknown`，不得伪造。

## 4. 系统与数据约束

- 采用前端与后端分层的开发模式，具体框架和技术选型由技术路线文档单独确认。
- 公共业务不得依赖某个平台的私有字段；平台差异必须收敛在采集器适配层。
- 每周计划节点只保存星期、24 小时制时刻、启用状态和所选自动提取规则；第一版自动提取规则只配置规则名称和各平台每圈有效结果目标数，不选择平台、车型或圈子；平台采集配置只保存接入状态、平台内部总并发、启用状态和 Session；车型下可以关联多个平台圈子，圈子独立决定是否参加自动提取。任一配置项只能在一个配置区域编辑，其他区域只能引用或只读展示。
- 自动提取规则修改时生成新版本，未来计划触发使用最新版，已创建批次保留触发时版本快照；存在任何计划节点引用时不得归档或删除，从未产生批次且无引用的规则可以永久删除，已有历史批次的规则只允许归档并保留恢复能力。
- 所有启用的每周计划节点按“星期 + `HH:mm:ss`”全局唯一；多选星期逐日展开检查，停用节点允许暂存冲突但重新启用时必须通过校验，系统不得自动合并含义可能不同的节点。
- 第一版尚未正式交付且没有需保留的业务数据库，自动提取规则与每周计划节点直接进入全新数据库基线；不实现旧 `times` 或平台自动数量的运行时兼容转换，也不自动创建默认规则。
- 后续新平台接入后默认停用；启用前必须补齐所有启用计划节点所引用规则中的该平台数量，缺失参数只在“提取计划”编辑，“平台配置”仅显示错误摘要和跳转入口。调度器发现启用平台缺失规则参数时停止该节点本次触发，不静默跳过平台创建不完整批次。
- 手动提取与定时提取必须复用同一采集流程。
- 每次执行都创建独立提取批次；不同批次的数据不更新、不合并。
- 同一批次内部可以按平台帖子 ID 或 URL 去重。
- 必须保留平台原始状态，标准化状态只能是 `visible`、`hidden` 或 `unknown`。
- Cookie、令牌及其他敏感凭证不得进入前端列表、日志、示例数据或 Git。
- 采集必须使用有权访问的数据和账号，遵守适用的平台规则；遇到限流或验证要求时不得持续强行请求。

## 5. 开发与验证规则

- 开始实现前先确认需求边界、验收条件和目标平台样本。
- 不确定的平台接口、分页规则、状态字段和反爬行为必须先做验证，不得写成已确认事实。
- 新增平台必须实现统一采集器契约，并通过公共行为测试和平台样本测试。
- 动代码必须更新 `WORKLOG.md` 最新条目；纯讨论、只读调研或未改代码的任务不记账本。
- 功能变更必须同步更新相关 owner 文档和账本，但不要把详细过程写回本文件。
- 提交前至少执行：相关自动化测试、静态或构建检查、`git diff --check`，并确认无凭证和无关文件进入提交。
- 验证强度必须符合 `docs/process/verification-ladder.md`；构建、单测或子 Agent 自报通过不能单独构成目标场景验收。
- 所有本地 PoC 输入、测试包和原始结果统一放在 `artifacts/poc/`：甲方真实清单位于 `inputs/`，测试包位于 `packages/`，各候选各轮结果位于 `results/<candidate>/<round-id>/`。该目录不得提交 Git；Git 只保留模板、汇总报告、评审结论、ADR 和用于追溯的校验值。测试包和原始结果至少保留到第一版验收完成，之后由项目负责人手动决定是否删除。
- 若仓库尚未建立测试工具，必须在账本中明确记录实际完成的人工验证及尚未覆盖的风险。

### 5.1 唯一账本与恢复

- 根目录只保留一份 `WORKLOG.md` 作为活动任务账本；不得新增第二份当前进度文档、状态看板或平行 TODO。
- 最新条目放在最上方，每条只记录总目标、状态、带证据的当前进度、精确下一步、必要边界和关联身份。
- “已完成”必须附命令、关键输出、产物路径、提交或 PR 等可复核证据；只有结论没有证据不算完成。
- 只有用户能决定的阻塞项，同时写入所属 owner 文档，并在账本顶部固定的“待你裁决”小节追加一行；用户决定后划掉。Agent 自己能解决的技术问题不进入该小节。
- 详细日志和多 Agent runtime artifact 放在被 Git 忽略的 `artifacts/runtime/`，不得把长过程复制进账本。

### 5.2 反过度工程

- 调研充分、证据完整、验证严格；最终实现采用满足当前需求的最简单可靠方法。
- 优先复用现有页面、接口、数据结构和约定；创建新机制前先检索仓库是否已有同类能力。
- 不为“以后可能用”新增页面、配置项、开关、接口、字段、表结构或投机性抽象。
- 可配置面保持最小，只有真实业务或运维需要变化的值才进入配置。
- 精简约束实现范围，不得以“控制成本”为理由削弱已确认的验收证据。

### 5.3 多 Agent 与长任务

- 不因任务规模大而自动并行。先确定当前阶段门、关键路径、写 owner、必要分支和验证路径。
- 只有至少两个关闭当前阶段门所必需、前置稳定、写目标不冲突、可独立验证且并行净收益为正的分支，才使用多 Agent。
- 调查可按不同证据切片并行；实现默认单写手。确需并行写时，每个写目标只有一个 owner，并使用独立 Git worktree 物理隔离。
- 共享工作树禁止使用 `git add -A` 或 `git add .`；只暂存本任务精确文件，并在提交后核对 `git show --stat`。
- 子 Agent 按 `docs/process/agent-receipt-template.md` 返回短回执，必须说明基线、覆盖范围、分母、失效条件和可复核入口；详细过程留在 runtime artifact。
- 同模型多路结果不按票数裁决；主线程依据证据决定，并在整合后真实触发组合目标路径。
- 子任务失败或超时不得静默丢失覆盖，必须明确当前阶段门继续阻塞，或声明缺口后继续。
- 并发数和资源上限必须显式受控；协调 Agent、怀疑者 Agent 和分层汇总只在确有收益时使用。

## 6. Git 功能分支工作流

### 6.1 分支策略

- 除仓库首次初始化外，不直接在 `main` 上开发功能。
- 开始任务前同步 `main`，从最新 `main` 创建独立分支。
- 分支命名：
  - 功能：`feat/<简短名称>`
  - 修复：`fix/<简短名称>`
  - 文档：`docs/<简短名称>`
  - 重构：`refactor/<简短名称>`
- 一个分支只处理一个明确范围，提交信息应简洁说明实际变更。
- 不覆盖、不清理用户已有且不属于当前任务的修改。
- 本地执行 `scripts/install-git-hooks.ps1` 或 `scripts/install-git-hooks.sh` 启用版本化 pre-commit 兜底；hook 只检查提交阶段，不能替代账本恢复纪律。

### 6.2 自动收尾授权

当开发任务满足以下全部条件时，视为已获得自动完成 Git 收尾的项目级授权，不需要重复询问是否提交、推送、合并和删除分支：

1. 需求与验收范围已经完成；
2. 相关测试和检查通过；
3. 设计、相关链档和 `WORKLOG.md` 已同步；
4. 工作区中不存在来源不明或超出范围的修改；
5. 不包含密钥、Cookie、令牌、个人数据或大体积临时文件；
6. 功能分支与目标分支之间不存在未解决冲突；
7. 远程仓库认证及分支权限允许操作。

满足门槛后执行：

1. 提交当前任务范围内的变更；
2. 推送功能分支；
3. 优先创建 Pull Request，并在检查通过后合并到 `main`；
4. 若仓库未启用 Pull Request 流程，可在确认无冲突后按仓库实际能力完成等价合并；
5. 推送合并后的 `main`；
6. 删除已经合并的远程功能分支；
7. 切回并同步本地 `main`，删除已经合并的本地功能分支；
8. 最后确认本地 `main`、远程 `main` 和工作区状态一致。

出现测试失败、冲突、权限不足、敏感信息、无关修改或用户明确要求暂停时，必须停止自动收尾并报告阻塞原因，不能强行合并。

## 7. 文档维护边界

- `AGENTS.md`：只保留目标、稳定规则、长期边界和工作流。
- `WORKLOG.md`：唯一活动任务账本，最新条目在最上方；不堆积完整规范或长日志。
- `product-design.md`：描述产品功能、业务规则、阶段范围和验收要求，不写具体框架。
- `technical-route.md`：只在技术路线正式确认后记录框架、运行方式、依赖和关键技术决策。
- `docs/chains/`：每条跨任务工作线一份链档，保存当前口径、每任务一行的决策链和带解锁条件的未决项。
- `docs/memories/`：一事一文件，只记录已证实的根因、实际踩过的坑和下次最快杠杆。
- `docs/project-notes/`：只保存确有价值且已冻结的历史记录，不声明当前状态或下一步。
- ADR：记录已接受的重要决策及替代关系，不直接删除历史决策依据。
